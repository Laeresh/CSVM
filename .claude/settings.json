{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$j = [Console]::In.ReadToEnd() | ConvertFrom-Json; if ($j.tool_input.command -match 'RunTests\\.ps1|dotnet\\s+test') { dotnet format 'CSVM/CSVM.csproj' --verbosity quiet }; exit 0",
            "timeout": 300,
            "statusMessage": "dotnet format before tests..."
          }
        ]
      }
    ]
  }
}
