# CLAUDE.md

## ⚠ Keep this file + `docs/` up to date

**This file is the compact, authoritative index of project context for Claude; deep detail lives in `docs/`.** Update documentation in the same turn as the change it describes:

- Changes to *what the tool does from the outside* (flags, outputs, defaults, algorithms, UI, entry points) → the relevant section **here**.
- Implementation detail, diagnosis narratives, verified gotchas → the module's bullet in `docs/architecture.md`. **Read a module's bullet there before modifying that module** — dead ends and misdiagnoses are recorded so they don't get re-chased.
- Format / reverse-engineering knowledge → `docs/formats/`.
- Landed work → a dated entry appended to `docs/HISTORY.md`, plus refresh "Current status" here (current state + next step only — it is not a log).
- Pure refactors with no external effect → usually no update needed. When in doubt, update.

## Project Description

An XWVM-style remake of **Crimson Skies** (2000, Zipper Interactive, Microsoft): a modern engine that plays the original game using the player's own legally-owned game files. Nothing like this exists yet for Crimson Skies — this is a first-of-its-kind effort.

Charter decided 2026-07-14 (full detail in Claude's project memory):

- **Milestone 1** — complete asset extraction: fill the Crimson Skies gaps in mech3ax (`planes.zbd`, `gamez.zbd`).
- **Milestone 2** — vertical slice: free flight only. One plane, one map (candidate: C1 instant-action arena), arcade controls, original sounds. No AI, objectives, or weapons.
- Long-term direction (not commitment): full campaign remake.

## 🚫 Hard rule: no game assets in version control — ever

Public open-source project under the **XWVM legal model**: the repo ships **code and format documentation only**. Game files, extracted assets, ZBD contents, screenshots of hexdumps containing bulk asset data — none of it gets committed or published. The importer reads the player's own install at runtime. `.gitignore` enforces this for `CrimsonSkiesGame/`, `extracted/`, and `tools/`; keep it that way when adding directories.

## Architecture & key decisions

- **Engine:** Godot 4 .NET (C#). Consumes extraction output via mech3ax / Mech3DotNet (strongly-typed C# wrapper).
- **Format reverse engineering:** happens in a fork of [mech3ax](https://github.com/TerranMechworks/mech3ax) (Rust), PR'd upstream to TerranMechworks. Their byte-identical round-trip test harness (extract→repack) is the correctness standard.
- **Flight model:** data-driven approximation — parameterized by plane stats from extracted zrdr reader files, hand-tuned against the original game. No exe decompilation.
- **Division of labor:** Claude writes the Rust parsers, Godot code, and docs; the user reviews, playtests flight feel, and owns upstream/community communication.

## Repo layout

- `CrimsonSkies/` — the Godot 4 .NET project (the actual remake; committed). See "Godot project" below.
- `CrimsonSkiesGame/` — the user's retail game install (git-ignored). ZBD archives in `CrimsonSkiesGame/ZBD/` organized as campaign chapters `C1`–`C5`, each with instant action (`IA1`), story missions (`M0x`), multiplayer maps (`MP1`–`3`). Cutscenes are plain MPGs in `CrimsonSkiesGame/GOSDATA/ASSETS/GRAPHICS/MPG/`.
- `extracted/` — extraction output workdir (git-ignored). Populated by `ExtractAssets.ps1` (below), which mirrors the game's ZBD folder structure: top-level `planes.zip` (unzbd of planes.zbd), `zrdr.zip`, `soundsh.zip`/`soundsl.zip`, `interp.json`, `rimage.zip`, plus per-chapter `C1/gamez.zip`, `C1/texture.zip`, `C1/rtexture*.zip`, `C1/zrdr.zip`, and per-mission `C1/IA1/zrdr.zip` etc. The viewer's defaults read `planes.zip`, `C1/gamez.zip`, `C1/texture.zip`, `zrdr.zip`, `soundsh.zip` — but for each default it **prefers the unpacked sibling folder when present** (e.g. reads `extracted/C1/texture/` over `C1/texture.zip`), so running `ExtractAssets.ps1 -Unzip` (or unzipping just the archives you want to grep in the editor) makes the viewer load loose files and skip zip decompression. All four loaders (`GameZ`, `TextureArchive`, `Zrdr`, `SoundArchive`) accept a zip or a directory; an explicit `--gamez=`/`--textures=`/`--zrdr=`/`--sounds=` is used verbatim. **The `rtexture*`/`rimage` archives are NOT loaded, by design (verified Run-2 item 2):** each `rtextureN.zip` is a **downscaled quality tier** of the same texture set, not a hi-res replacement — measured across all 896 C5 textures, `texture` == `rtexture14` (both max-res), `rtexture2` = ¼, `rtexture4/6/8` = ½, and no rtexture file ever exceeds `texture`; so the base `texture.zip` the viewer loads is already the highest resolution. `rimage.zip` is the UI/HUD image set (crosshairs, buttons, cursor, menu splash, briefing thumbnails), no world geometry textures.
- `ExtractAssets.ps1` (repo root) — bulk extractor: walks `CrimsonSkiesGame/ZBD`, runs `unzbd cs <mode>` on every ZBD with the right mode for its type (interp→interp/.json; planes+gamez→gamez; soundsh/soundsl→sounds; zrdr→reader; rimage/texture/rtexture*→textures), and writes the output to the mirrored relative path under `extracted/` (basename kept, extension → `.zip`, or `.json` for interp). `cam_anim.zbd`/`mis_anim.zbd` are reported+skipped (mech3ax has no CS anim support). Idempotent (skips outputs newer than their source unless `-Force`); `-Unzip` also expands each `.zip` into a sibling folder; `-Source`/`-Dest` override the roots.
- `tools/` — downloaded binaries (git-ignored): mech3ax v0.6.1, Godot 4.7 .NET editor at `tools/godot/Godot_v4.7-stable_mono_win64/` (`*_console.exe` for CLI use).
- `docs/PLAN-M2-polish.md` — the agreed Milestone 2 polish plan (ordered work items with goal/evidence/approach/verification + status checklist); keep its checklist in sync as items land. **All 8 items done (2026-07-17).**
- `docs/PLAN-M2-polish-2.md` — Milestone 2 polish **Run 2** (planned 2026-07-17 after a grilling session; same format + ground rules): 13 ordered items from NOTES.md's "Polishing Run 2" section + the open Issues bullets. Keep its checklist in sync as items land.
- `OriginalScreenshots/` — user-captured reference screenshots from the original game (committed; UI/plane/sky references for fidelity comparisons — contains no bulk asset data).
- `docs/HISTORY.md` — chronological Milestone-2 development log (moved out of this file 2026-07-18): every landed change with its verification details. Append new dated entries there when work lands.
- `docs/architecture.md` — deep per-module implementation notes for `CrimsonSkies/src` (the long narratives formerly inline in this file): read a module's bullet before touching it; update it in the same turn as a change.
- `docs/formats/` — validated format documentation (`gamez.md`, `world-structure.md`, `zrdr.md`), moved from this file 2026-07-18; the seed of Run-2's public `docs/formats/` reference item.

## Godot project (`CrimsonSkies/`)

Godot 4.7 .NET, C# / net8.0. Build & run:

```
dotnet build CrimsonSkies/CrimsonSkies.sln
tools/godot/.../Godot_v4.7-stable_mono_win64_console.exe --path CrimsonSkies res://scenes/Main.tscn -- --plane=player_bhawk
```

(First time only: run with `--headless --import` once before running scenes.)

Compact module index — **deep implementation notes, verified diagnoses, and dead ends for every module live in `docs/architecture.md`; read that module's bullet before changing it.**

- `src/Mech3/GameZ.cs` — loads a mech3ax GameZ extraction (zip or unpacked dir): nodes/meshes/materials JSON → plain C# objects, incl. World partition refs, per-corner vertex colors, mesh `lights` arrays, per-polygon draw priority (`unk04`/`priority`, both keys), each node's flat `Index` (= original draw order), and the World's `area` ground-plane bounds + partition-grid dims (for WorldBuilder's map-edge continuation).
- `src/Mech3/TextureArchive.cs` — texture lookup (zip or dir), handles the two name quirks; classifies alpha per texture (`LastHadAlpha`/`LastAlphaIsSoft` — soft alpha must blend, scissor destroys it). Missing names logged once (plain lines, never `GD.PushWarning`); known-absent-from-game-data textures (`pir_spinner`, `barngrill`) render neutral gray, so magenta = our bug.
- `src/Mech3/SceneBuilder.cs` — shared GameZ-subtree → MeshInstance3D builder: n-gon triangulation, nearest LOD, one surface per (material, priority). Replicates the original's coplanar layering as scale-invariant depth bias (priority × 2e-4 + within-mesh surface rank × 2e-6 + node index × 5e-8 as an `instance uniform` — needs Forward+; `project.godot` raises the global-shader-variable buffer to 262144 for C4/C5 instance counts). Soft-alpha textures auto-blend, others scissor; `billboardTexture` predicate for cloud sprites (recentered on quad centroid; own billboard `ShaderMaterial` that shares the distance fog — item 4); mesh point-lights as camera-facing soft glow sprites (own point shader: gaussian falloff, ×1.6 gain, distance-sized 3–30 px; per-light decoded size/range params — range fade keeps beacons inside the fog, skydome stars exempt via `csky_light_fade`); `glowTexture` predicate → the world's single-quad `*flare*` lamp meshes (refinery/pier/lighthouse) billboard via a glow shader variant (always blend, no night dim, non-collidable — flare polys inside regular geometry and multi-poly flare strings stay static); optional trimesh collision + `collisionSkip`; `cullBackfaces` honors SHOW_BACKFACE via `cull_front`; cylindrical distance fog (horizontal distance × altitude fade) via global `csky_fog_*` params + per-instance `csky_fog_on` opt-out. **Fullbright world brightness (item 6):** the `!shaded` branch multiplies texture × vertex colour in **gamma (sRGB) space** (linearises COLOR first via `SrgbToLinearFn` — matches the original DX7 `D3DTOP_MODULATE`, fixes washed-yellow → saturated-green terrain) and dims ALBEDO by the global `csky_world_light` scalar (the mission's SUNLIGHT brightness, before the fog mix). Planes (shaded) keep raw COLOR + full brightness. Same two fixes in the cloud-billboard + Clutter shaders.
- `src/Mech3/PlaneBuilder.cs` — builds one aircraft (shaded, `cullBackfaces: true`): skips cockpit/destroyed/shadow/`*_hook` subtrees and `pdpN`/`pcdpN` damage panels + `player_damage_off` (keeps the healthy `pdpN_h` panels — skipping them amputates wingtips). `spinningProps` toggles static disc vs blur layers; `*blur*` textures alpha-blend (scissor erases them). Wing flares built hidden, re-skinned as additive amber billboards, exposed as `WingFlares`.
- `src/Mech3/PropParts.cs` — classifies prop/rotor nodes by name; spin axis + rate from the original anims (props local Z, −220/+60; autogyro rotor local Y, +165/−60; treated as deg/s, TUNE).
- `src/Mech3/ControlSurfaces.cs` — classifies aileron/elevator/rudder mesh nodes (deflecting child under hinge-parent group; parents must NOT classify) + hinge axes (local X ailerons/elevators, local Y rudders). No deflection data exists — angles/rates are TUNE in the animator.
- `src/Mech3/WingLights.cs` — single source of truth for wingtip nav lights: flare node names, `oil_liteflare` glow texture, warm-amber color, 1.5 s blink period. Treatment scoped by node name, not texture (texture is shared with airframe meshes).
- `src/Mech3/WorldBuilder.cs` — builds a chapter world (fullbright): World children + partition-referenced subtrees; skips `horizon`/`fvol*`/`dzpaths`. Cloud/sky classified by **texture** (blend + billboard the sprites, exempt from collision); the opaque `cloudlayer` deck split into a separate `CloudDeck` node for the player-follow. `BuildHorizon(zone)` = camera-anchored skydome (zone2 night default; billboarded color-keyed moon; participates in fog since the 2026-07-17 cylinder remodel — `DisableFog` is disabled by an early return). `CreateEdgeExtender(clutter)` (after Build) returns the item-7 `MapEdgeExtender`.
- `src/Mech3/MapEdgeExtender.cs` — **map-edge continuation (item 7):** a rolling window of repeated border tiles + clutter following the camera past the map boundary indefinitely, like the original's tile-reload grid (user video + in-game tests). Each axis outside the map **clamps to its border cell**, repeated forever and **alternately reflected** (every seam a shared mirror plane — heights match exactly; the interior never repeats, so no phantom airport; sea edge → sea forever, forest → forest). The reflection is our seam-free construction, likely NOT what the original does (user: plain repetition — open fidelity question, NOTES.md; one-line swap in `MirrorAxis`). Window = cells within `Rings`=5 of the camera cell (covers raw fog-far + a margin ring so the original's visible reload pop never shows), re-diffed only on 1024 m cell crossings; cells = bare ground leaves (no buildings) + the source cell's clutter sprites at mirrored positions, both collidable in flight. Active in `--fly` and static `--sky-zone` views; off for plain orbit viewing.
- `src/Mech3/Clutter.cs` — the original's clutter system: interp.json `AddClutterTemplates` templates (ground quad whose texture names the terrain it decorates, size = tiling period) stamped across matching-textured world polygons on a fixed world-space grid (C1: 9,303 tree/bush sprites). One Y-billboard MultiMesh per decoration kind, fullbright + fogged (+ item-6 gamma-space vertex modulate + `csky_world_light` SUNLIGHT dimming, like the world); crossed-quad trimesh colliders in flight. C2/C5 non-quad city-block templates logged + skipped. `ExportedKinds` feeds MapEdgeExtender (same trees on the repeated border tiles).
- `src/Mech3/Zrdr.cs` — zrdr extraction reader (zip or dir) + `ZrdrDict` view over alternating key/[values…] lists (collapses duplicate keys — anim defs walk raw lists instead); `LoadMatchingFiles` enumerates an archive's readers by content sniff.
- `src/Mech3/AnimDefs.cs` — ANIMATION_DEFINITION loader (item 8): parses every def in a zrdr archive (name/root/activation, RESET_STATE, sequences) keeping only the four *state* op kinds (active/translate/rotate/motion-end-pose); playback ops recorded + skipped (part 2). Schema: `docs/formats/anim-definitions.md`.
- `src/Mech3/MissionState.cs` — mission start-state applier (item 8): at world build applies anchored defs' RESET_STATEs (healthy ACTIVE / destroyed INACTIVE — the coplanar-flicker fix), ON_STARTUP sequences (zepstate.json — C1/IA1 hides `dliner1`/`cargotrain`), startanims.json NEW_GAME_START end-states in order (hangar 3 doors end open), then a logged safety net hiding uncovered `*destroyed*` subtrees (name-contains only — a `*_dest` suffix rule wrongly hid `ref_tank_dest`, the healthy harbor tank GROUP; reverted). INACTIVE = hidden + colliders disabled. Anchors by wildcard NAME (`*`/`**`/`#`, `.flt` optional) or ANIMATION_ROOT_NAME→parent lift capped at 16 matches (generic `healthy` roots = per-object anims, never world-anchored); resolves via the `cs_name` meta (original gamez names — Godot mangles duplicates). Runs in every world build (fly + static). Scenarios don't change world state (analysis in the format page).
- `src/Mech3/WavFile.cs` — pure-C# WAV parser + MS ADPCM→PCM16 decoder (the game's format; Godot can't load it).
- `src/Mech3/SoundArchive.cs` — WAV lookup over a sounds extraction → cached `AudioStreamWav` (forward loop when LOOPED).
- `src/Mech3/SoundDefs.cs` — sounds.json SETS parser: `snd_*` → `SoundDef` (wav, flags, range, volume).
- `src/Flight/PlaneStats.cs` — typed per-plane flight stats from vehicle.json (`kind_of` chain) + engines.json + player.json globals, incl. engine-sound def + volume/pitch curves.
- `src/Flight/SpawnPoints.cs` — flight spawns from the **mission's own zrdr**: `LoadIa` (ia.json `spawn_points`, random pick) with `LoadPlayerInit` (objectives.json PLAYER_INIT) as story-mission fallback.
- `src/Flight/Weather.cs` — `WeatherState` from the mission's weather.json: per-zone fog (color/ranges/altitude band), per-zone **`SUNLIGHT_*`** → `ZoneFog.WorldLight` (item 6: `clamp(AMBIENT + DIFFUSE·0.46, 0.15, 1)`, the per-mission fullbright-world brightness — C1 0.80 / C1B night 0.43 / day →1.0 — drives PlaneViewer's `csky_world_light`), `CLOUD_COVER` whiteout trapezoid (`WhiteoutAmount`) + optional deck-face tints (`CloudTopColor`/`CloudBottomColor`, decoded, unused), `WIND`, and the `Precip` block (`PrecipData`: SNOW/RAIN + tint/wind/gravity/particles/alpha — item 5). The CLOUD_COVER/WIND/precipitation blocks pair keys with bare scalars — walked raw (`ScalarAfter`/`StringAfter`/`ListAfter`/`Vec2After`), not via `ZrdrDict`. **Color triples are dual-encoded** (verified across all weather.json): normalized floats (C1 `FOG_COLOR` 0.69) OR integer 0–255 (C4/C5 `FOG_COLOR` `[192,192,192]`; every `TOP_COLOR`/`BOTTOM_COLOR`, precipitation `COLOR`) — `ParseColor` divides by 255 when any component >1 (a component of exactly 1.0 is a float), then PlaneViewer treats the result as a DX7 sRGB value and converts to linear. See `docs/formats/weather.md`.
- `src/Flight/FlightAudio.cs` — own-plane loops: engine (throttle→pitch 0.6→1.0, 1.8 s start ramp behind `snd_propstart`), overspeed whine, rattle; `OnCrash()` explosion one-shot; `OnEngineStop()` wired but unused (future landing flow).
- `src/Effects/Puffer.cs` — data-driven `PUFFER_STATE` billboard-particle emitter (crash `large_fireball`): CPU sim honoring the reader's params + flipbook, one additive MultiMesh. Reusable for dogfight effects.
- `src/Effects/CloudPuffs.cs` — synthetic ambient cloud-puff field (the world's own sprites cluster near the airfield only): pool of soft billboards Y-anchored to the CLOUD_COVER band, following the plane in X/Z, recycling to the leading edge; one alpha-blended MultiMesh, fogged with the same cylindrical term as the world (item 4 — small effect: the shell sits inside the fog near-range); every feel constant TUNE.
- `src/Effects/Precipitation.cs` — rain/snow from the weather.json precipitation block (item 5; C4 snow, C1C/C2B rain — data-driven type/tint/gravity/wind/particles/alpha). One camera-following MultiMesh the plane flies through: each instance carries a fixed random seed, and a spatial shader positions it from `TIME` + `CAMERA_POSITION_WORLD` (fall + drift, wrapped into a camera-centred box) — **self-animating, zero per-frame CPU** (a world-sized custom AABB keeps it from being frustum-culled). SNOW = camera-billboard flakes with a per-instance flutter; RAIN = fall-direction-aligned streak quads (length ∝ fall speed). Near-fade kills the on-lens blob for the chase cam; no fog term (the ~30 m box sits entirely inside the fog near-range). Procedural sprites (soft dot / streak) — the original used untextured line primitives, asset-free here. Feel constants TUNE.
- `src/Flight/FlightModel.cs` — arcade **velocity-vector** flight model: thrust/drag/gravity integrate on the velocity vector (speed passes through zero); drag = quadratic + linear blend normalized so full-throttle equilibrium = `fd_speed`; lift fraction = speed² × wing verticality cancels cross-path gravity; nose-chase alignment scales with airspeed + wingVert; stall pulls the nose great-circle toward world-down and caps its elevation at the horizon; climb gravity ×0.6 (`ClimbGravityScale`). TUNE constants marked; known arcade artifacts accepted (loop energy pump, stable steep-climb equilibrium, no flight_ceiling).
- `src/Flight/PropAnimator.cs` — spins the collected prop/rotor discs about their local axes, throttle-scaled (idle floor 0.4); `--fly` only.
- `src/Flight/ControlSurfaceAnimator.cs` — absolute-pose surface deflection from slewed stick channels (±20°, slew 3 /s, both TUNE); per-surface sign bakes stick convention + Bloodhawk canard flip (hinge z < −1 m ⇒ TE-down on pull) + yaw-π frame flip.
- `src/Flight/WingLightBlinker.cs` — flashes the wing flares 0.08 s (TUNE) every 1.5 s; reset off on respawn; `--fly` only.
- `src/Flight/PlaneCollider.cs` — derives 3–4 plane-frame airframe boxes (fuselage / wing slab(s) / tail) from the built model's mesh triangles, geometric classification, no per-plane data (prop blur discs excluded; autogyro rotor disc = its wing). Known limit: Bloodhawk canard-tip slivers uncovered.
- `src/Flight/CompassTape.cs` — the original's top-center heading tape (`--fly` HUD), drawn from the game's own HUD textures (`compassticks2` tick tile + `compasstxt` label atlas — in **every chapter's texture archive**, user-found): a cylindrical drum seen edge-on, 180° visible (`x = center − R·sin(Δ)`, cos(Δ) brightness fade, no gain), headings increasing to the **LEFT** like a real whiskey-compass card — all metrics probe-matched to `OriginalScreenshots/HUD.png`. Ticks point-sampled / labels bilinear (child layer). North = −Z assumed (FlightController's heading line — unverified vs the original, one-line flip).
- `src/Flight/FlightController.cs` — flying-aircraft node: input (keyboard/gamepad, `--hold` scripted segments) → FlightModel → transform; roll-following chase camera (position lerp + **basis slerp**, full bank-follow, inverted flight shows the world upside down; snap on respawn); text HUD + compass-tape heading feed + 1/s telemetry (`path`/`nose`/`wv`); spawns at 0.5 throttle / 53.6 m/s. Collision: sweeps the PlaneCollider boxes via `CastMotion` each physics frame (center ray kept as anti-tunnelling backstop) → crash = named log line, explosion one-shot + `large_fireball`, airframe hidden, freeze until R (auto-respawn on `--hold`). Debug pause P = frozen sim + free orbit camera. Advances PropAnimator / WingLightBlinker / ControlSurfaceAnimator.
- `src/PlaneViewer.cs` — Main.tscn root script: mode selection from user args (below), orbit camera, lighting; F12 manual screenshot → `Screenshots/`, F11 prints the current camera pose as ready-to-paste `--campos=`/`--lookat=` args (any mode). In `--fly`: skydome scaled 2.5× re-centered on the camera every frame (zero-parallax backdrop, depth-sorts behind terrain); `SetupWeather` registers/sets the fog globals (`FOG_COLOR` converted **sRGB→linear**; `FOG_RANGES` halved by `fogRangeFactor` 2, TUNE) + the `csky_world_light` scalar (item 6: `WorldLight`, set **linearised** so the fullbright shaders' linear-space dim lands in gamma space), drives the whiteout overlay from camera altitude, re-anchors the `CloudDeck` at the whiteout-band centre under the player, builds + drives `CloudPuffs`, and builds `Precipitation` (self-animating, no per-frame driving). Static `--chapter` + `--sky-zone` enables the same weather for deterministic screenshots.

### User args (after `--`): 
`--plane=`, 
`--chapter[=C1]` (render a chapter's single `world1`; takes `C1`/`C1B`/`C1C`/`C2`/`C2B`/`C3`/`C4`/`C5` — drives the default gamez to `<chapter>/gamez.zip` and textures to `<chapter>/texture.zip`; the world node name is always `world1`), 
`--fly` (free flight: chapter world + skydome + plane + arcade controls — WASD/arrows pitch+roll, Q/E rudder, Shift/Ctrl throttle, R respawn, P pause (debug screenshot freeze), Esc quit; or gamepad; defaults to C1, combine with `--chapter=` for another chapter; also builds the crash fireball via `PufferState.Load`/`Puffer.Create` while the texture archive is open, handing it to the FlightController; spawns from the mission's ia.json — see `--mission`/`--scenario`/`--spawn`), 
`--mission=IA1` (which mission's spawns `--fly` uses; IA1 reads `ia.json` spawn_points, M0x story missions read `objectives.json` PLAYER_INIT — pair with the matching `--chapter=` so the world fits the spawn), 
`--scenario=zeppelin_run` (which instant-action scenario's spawn list — `zeppelin_run`/`dogfight_ace`/`dogfight_squadron`/`stunt_flying`/…), 
`--spawn=N` (force spawn index N in that list; default is a random pick per launch like the original, and the chosen spawn is logged), `--sky-zone=zone1|zone2` (horizon zone; default zone2 = night — also selects that zone's distance fog, and enables fog+whiteout + the map-edge border extension in static `--chapter` mode for deterministic weather / edge-continuation screenshots), 
`--gamez=`, 
`--textures=`, 
`--zrdr=` (default extracted/zrdr.zip), 
`--interp=` (default extracted/interp.json — the boot scripts naming each chapter's clutter templates; every world build stamps them via `ClutterBuilder`, logged as `clutter: N sprites`, solid only in `--fly`), 
`--sounds=` (default extracted/soundsh.zip), 
`--mute` (skip flight audio), 
`--debug-collision` (draw the plane's collision probe), 
`--hold=p,r,y,thr[@dur][;…]` (scripted flight input for automated runs — `;`-separated segments with `@seconds` durations sequence inputs, e.g. `1,0,0,0@1.1;0,0,0,0` = pull 1.1 s then release; a single segment = the old constant hold), 
`--yaw=`, 
`--pitch=`, 
`--campos=x,y,z` / `--lookat=x,y,z` (manual camera placement), 
`--screenshot=<path>` (render a few frames, save PNG, quit — used for automated visual verification).
`--frames=N` (delay before screenshot), 
`--shots=N` (after the `--frames` warm-up, capture N **consecutive** frames — one per `_Process` — then quit; default 1 = the single-shot behavior at the verbatim path; N>1 writes zero-padded indexed files `foo.png`→`foo_00.png`/`foo_01.png`/… via `IndexedShotPath`, for **z-fighting debugging** since the flicker is only visible across frames), `--jitter=<deg>` (per-frame camera dither during a `--shots` burst — micro-orbits the eye around the framed point (`_orbitCenter`) so a dead-still camera doesn't render bit-identical frames; default 0.15° when `--shots>1` else 0, `0` disables; static mode only — in `--fly` the FlightController owns the camera and the plane's motion already surfaces the fight; file `_00` is the un-jittered baseline), 

### Format gotchas (the ones that bite constantly)

- `nodes.json` `children`/`parent` are **flat list positions**, NOT the `node_index` field (node_index has duplicates).
- Euler `transformation.rotation` composes **R = Ry(y)·Rx(x)·Rz(z)** = Godot's `EulerOrder.Yxz`; when `matrix` is present it wins and is stored **transposed**.
- Coordinates are right-handed Y-up with the nose at **−Z** — Godot's frame exactly; no mirroring, no UV V-flip.
- `meshes.json` has `null` entries — keep them to preserve `mesh_index` alignment.
- Texture name quirks: fixed-width 20-char truncation (prefix-match) and mech3ax duplicate renames (strip `.-N` suffix). Plane skin pixels are in each chapter's `texture.zbd`, not planes.zbd.
- Per-corner `vertex_colors` = **baked lighting** (really an AO/shadow mask — 70% pure white, 30% darker): the world renders fullbright (texture × vertex color); shaded comes out murky-dark. Some polys are baked pure black on purpose (painted shadows). **The multiply is gamma-space (item 6):** the original DX7 engine multiplied texture × vertex color in sRGB space; our linear-space multiply washes out the baked-dark corners, so the fullbright shaders linearise the vertex color first (`SrgbToLinearFn`). On top of the vertex colors the original applies the mission's **SUNLIGHT** (weather.json) as a per-mission brightness — the remake's `csky_world_light` (item 6).
- Polygons carry a signed **draw priority** (v0.6.1 `unk04`, upstream `priority`); equal priorities resolve by **draw order, later wins** (polygon list order within a mesh, flat nodes.json order across nodes). Ignoring either z-fights every decal — SceneBuilder maps both to depth bias.
- Polygon flag `unk2` (upstream `SHOW_BACKFACE`, bit 0) = double-sided; without it the original backface-culls. The visible side is the CCW loop = Godot's *back* face → `cull_front`.

Full validated format documentation lives in `docs/formats/`: `gamez.md` (GameZ/planes.zbd structure, aircraft trees, damage states, node kinds), `world-structure.md` (chapter worlds, skydome zones, clutter templates, point-sprite lights), `zrdr.md` (flight stats, spawns, sounds, weather, effects survey, campaign mission↔folder map), `weather.md` (weather.json blocks, dual color encoding, SUNLIGHT), `hud.md` (compass-tape textures + drum projection).

## Format support status (validated against THIS install with mech3ax v0.6.1, 2026-07-14)

mech3ax's README support matrix is outdated — actual v0.6.1 support for CS is far better. All validation ran on this install (`unzbd cs …`, round-trip via `rezbd cs …` + sha256):

| Format | Status |
|---|---|
| `texture.zbd` / `rtexture*.zbd` / `rimage.zbd` | ✅ extracts to PNGs; round-trip **byte-identical** (C1 verified) |
| `soundsh.zbd` / `soundsl.zbd` | ✅ extracts to WAVs |
| `zrdr.zbd` (reader/mission config) | ✅ extracts to JSON (ai, engines, Briefing, …) |
| `interp.zbd` | ✅ extracts to JSON (engine boot scripts) |
| `gamez.zbd` (world geometry) | ✅ extracts (metadata/textures/materials/meshes/nodes JSON); round-trip **byte-identical** (C1 + C5 verified) |
| `planes.zbd` (aircraft models) | ✅ extracts — it's a GameZ-format file (the boot script loads it via `GameZReadZBDFile`). Round-trip differs by only 72 bytes / 6 MB: swapped `\0`/`.` garbage past the null terminator in fixed-width texture-name fields. Semantically lossless; upstream fix candidate. |
| `cam_anim.zbd` / `mis_anim.zbd` | ❌ unsupported by mech3ax — but surveyed 2026-07-18 (Run-2 item 9): MW3 `anim.zbd` format family (same signature, version 53 vs 39); chapter `cam_anim.zbd` holds the compiled chapter+shared anims incl. every vehicle `.zan` SI motion script (mission `mis_anim.zbd` only the mission's zep defs). Container + SI translate-cubic frames decoded and documented in `docs/formats/anim-definitions.md`; extraction = future mech3ax extension (upstream HEAD has dropped CS gamez support — fork must handle) |

Extracted plane data confirmed usable: `nodes.json` has 3,317 nodes including full hierarchies for `player_bhawk`, `player_peacemaker`, `player_kestrel`, `player_autogyro`, `player_avenger`, `player_balmoral`, `player_fury` with control surfaces (ailerons/elevators), props, gear, firepoints, cockpits.

## Current status / next step

(Current state + next step only. The full chronological log with per-item verification details is `docs/HISTORY.md` — append landed work there.)

- **Milestone 1 (extraction): essentially delivered by mech3ax v0.6.1.** Remaining RE work: the cosmetic planes.zbd padding nit (upstream PR candidate) and the deferred anim formats (`cam_anim`/`mis_anim` — not needed for free flight).
- **Milestone 2 core is complete** (2026-07-14 → 07-17): textured planes + chapter worlds render; `--fly` free flight over any chapter with data-driven spawns, arcade velocity-vector flight model, original sound (engine/whine/rattle/prop-start/explosions), swept airframe-box collision + crash sequence (fireball), night skydome (moon/stars/beacons), full weather (cylindrical fog, whiteout, player-following cloud deck, ambient puffs), forest clutter, spinning props, control-surface + wing-light animation, roll-following chase camera. **All 8 items of `docs/PLAN-M2-polish.md` are DONE (2026-07-17).**
- **Polishing Run 2 in progress: `docs/PLAN-M2-polish-2.md`** (13 ordered items, planned 2026-07-17 — key evidence pinned during planning is recorded in the plan + HISTORY). Done: item 1 (log hygiene — warn-once, no stack traces), item 2 (C4/C5 instance-uniform buffer overflow fixed via `project.godot` `buffer_size=262144`; anisotropic ground filtering — **pending user fidelity sign-off, trivially reverted**), item 3 (C4 white fog — `Weather.ParseColor` normalizes the dual float / int-0–255 `FOG_COLOR` encoding, verified C4 fog = 192-gray with a soft ramp / C1 unchanged = 176; `TOP_COLOR`/`BOTTOM_COLOR` decoded for item 6; seeded `docs/formats/weather.md`), item 4 (clouds render through fog — cloud-sprite billboards became a fog-carrying `ShaderMaterial`, `CloudPuffs` got the fog term; C1 far sprites verified → 176.0 = FOG_COLOR / C4 → 192.0, near sprites stay bright, no-weather view crisp — puff effect small-by-data), item 5 (precipitation — `WeatherState.PrecipData` decodes the weather.json TYPE block, `src/Effects/Precipitation.cs` renders it as one self-animating camera-following MultiMesh (SNOW flakes / RAIN streaks); verified C4 snow + C1C rain render and animate, C1 no-precip clean, one draw call — **pending user in-game A/B**; `docs/formats/weather.md` precipitation section written), item 6 (night/overcast brightness — **two data-driven fixes**, validated against the user's matched C1/IA1 zone1 reference: (a) the fullbright world/clutter/cloud shaders now multiply texture × vertex colour in **gamma space** — the original DX7 `D3DTOP_MODULATE` — fixing washed-yellow → saturated-green terrain; (b) a per-mission **SUNLIGHT** world-brightness `csky_world_light` from weather.json (`WorldLight = clamp(AMBIENT + DIFFUSE·0.46, 0.15, 1)`, self-scaling C1 0.80 / night 0.43 / day 1.0) dims the fullbright world+deck+dome. Measured deck/sky 168 vs orig 169, terrain 59 vs ~57, fog exempt. The audit ruled out a tonemap/pipeline bug and the TOP/BOTTOM_COLOR & naive-sunlight leads; the one TUNE constant `SunIncidence 0.46` rests on the single overcast reference — pending night/day references to confirm the self-scaling), item 7 (**map edge continuation** — `src/Mech3/MapEdgeExtender.cs`, a rolling window of repeated border tiles + clutter following the camera past the map boundary indefinitely, replicating the original's tile-reload grid (user video `C1 IA1 Tile Loading.mp4` + in-game tests: the border tile repeats every crossing, the interior/airport never returns). Outside each axis the local border cell repeats forever, alternately reflected for seam-free heights; window = 5 rings around the camera cell, re-diffed on cell crossings; cells = bare ground leaves + mirrored clutter sprites, both collidable. Verified by 110–120 s corner-crossing flights (terrain + trees continuous, zero errors), a scripted crash INTO extension terrain, no-airport statics, C4/C5 clean). item 8 (**mission states, anim-state engine pt 1** — `src/Mech3/AnimDefs.cs` + `src/Mech3/MissionState.cs`: every world build applies the mission's animation base/start states from the shared+chapter+mission zrdr scopes — anchored RESET_STATEs hide the `destroyed` building/vehicle variants (the C1 airfield coplanar flicker), zepstate.json ON_STARTUP states remove the phantom zeppelins/trains (C1/IA1: `dliner1` out of its shed, `cargotrain` gone), startanims.json end-states pose the hangar-3 doors open; INACTIVE also disables colliders; logged safety net hides uncovered `destroyed` subtrees (C1: 53, per-object defs = pt-2 scope; same-day fix: the net's `_dest` suffix rule wrongly hid `ref_tank_dest` = the healthy harbor tank group — now `destroyed`-only). Verified by before/after screenshots (airfield destroyed-overlay gone, zeppelin shed empty, doors open) + C1/C4/C5/C1C/C2 fly-smokes, zero errors, ~3.9k state ops in 1.4 s; scenario→state analysis negative — scenarios pick spawns only; schema documented in `docs/formats/anim-definitions.md`. **Pending user in-game A/B** — zeppelin/train roster + full fly-over for anything legitimately missing). Item 9 (animated vehicles, anim-state pt 2) **DEFERRED 2026-07-18 (user decision):** the train's motion is SI-script data that exists only inside `cam_anim.zbd`, which mech3ax doesn't extract — waits for a mech3ax extension (survey + partial format decode recorded in `docs/formats/anim-definitions.md`; also see the format table row). **Next step: item 10 — collision damage model** (10a collider fit → 10b part HP + severity → 10c visible damage → 10d crash breakup).
- **Compass heading tape landed 2026-07-18** (user request outside the Run-2 plan): the original's top-center compass in `--fly`, rebuilt from the game's own `compassticks2`/`compasstxt` HUD textures as the drum projection measured from `OriginalScreenshots/HUD.png` (see `src/Flight/CompassTape.cs` + HISTORY). Open: north = −Z assumed; drum constants + nearest-tick look pending user A/B.
- **Open TUNE items pending playtest:** compass north convention + `TileOverscan`/`RimGain`/nearest ticks (above), `fogRangeFactor` 2 (predates the sRGB fix — re-A/B), flight-model constants (`StallNoseRate`/`KnifeAlignFloor`/`ClimbGravityScale`/`LowSpeedDragBlend`, plus splitting the user's `rotationTune` 2.0 per-axis against original measurements — Run-2 item), control-surface angles/slew, cloud-puff opacity/density, wing-light `FlashDuration`, collision feel vs building corners, cloud-deck brightness (~40 units lighter than original).
- **Backlog (not scheduled):** PLAYER_INIT fields [3]/[4] semantics + per-plane spawn speed, full `player_plane_destruct` crash choreography, sky UV scroll (`h_zone*scroll`, rate unknown), `flight_ceiling`, star twinkle + the remaining undecoded light fields (flags 523/…, the 0.17 float), world vehicle animations (trains, zeppelin props), visual prop spin-up/down.