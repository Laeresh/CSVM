# CLI reference, every viewer/flight argument

Full per-flag reference for the Godot project's user arguments, passed after `--`:

```
tools/godot/.../Godot_v4.7-stable_mono_win64_console.exe --path CSVM res://scenes/Main.tscn -- --plane=player_bhawk
```

`RunGame.ps1` (play) and `RunDev.ps1` (dev, with console prompts) forward these verbatim.

**Flight is the default.** Any content arg builds a *flight* unless
`--viewer` is present: `--plane=player_fury` flies the Fury and `--chapter=C4` flies over C4.
`--viewer` asks for the static inspection view instead, and is where the damage, livery and mesh
labs live. A bare launch (no content arg) shows the launchscreen. PROJECT_CONTEXT.md carries a
compact table of the day-to-day subset; this page is the long tail.

## Flag index

**Every flag below has exactly one bullet under `## Flags`, and that bullet is the description of
record.** Find the flag here, then read only its bullet, one flag is one line, so this returns the
whole thing. **Never read this file whole**.

```
Grep "^- .--collision" docs/cli.md
```

Names only, deliberately: a gloss here would be a second description of the same flag, and that is
exactly how `PROJECT_CONTEXT.md`'s day-to-day table drifted from this page. One flag, one description.

**Modes and content, what gets built**
`--viewer` · `--fly` · `--stunt` · `--vs` · `--vs-kills` · `--vs-time` · `--coop` · `--freecam` · `--anim-lab` · `--menu` · `--skip-intro` · `--intro` · `--chapter` · `--stage` · `--node` · `--plane` · `--players` · `--mission` · `--scenario` · `--ia` · `--campaign` · `--play-anim` · `--movie` · `--presentation` · `--force-builtin`

**Placement, where the subject starts and which way it faces**
`--pos` · `--direction` · `--lookat` · `--view` · `--spawn` · `--campos` · `--spawn-at` · `--spawn-dir`

**Capture, screenshots and the scripted-run workhorses**
`--screenshot` · `--export-gltf` · `--frames` · `--shots` · `--jitter` · `--no-focus` · `--no-vsync`

**Determinism, pin a run so two runs are byte-identical**
`--det` · `--no-det` · `--seed` · `--no-pads`

**Livery and paint**
`--paint` · `--paint-color` · `--paint-decal` · `--paint-seed` · `--rof`

**Weapons, ordnance and damage**
`--loadout` · `--rocket` · `--gun-select` · `--target` · `--fire` · `--fire-rockets` · `--incoming` · `--ai` · `--ai-attack` · `--ai-damage` · `--ai-targeting` · `--difficulty` · `--no-assist` · `--no-crash-loss` · `--generators` · `--zeppelins` · `--zep` · `--wake-turrets` · `--wake-generators` · `--infinite-ammo` · `--ammo` · `--weapon-lab` · `--weapon-mount` · `--weapon-fire` · `--weapon-cycle` · `--weapon-click` · `--weapon-target` · `--weapon-surface` · `--weapon-standoff` · `--weapon-camera` · `--weapon-test` · `--damage` · `--damage-hd` · `--destroy` · `--crash`

**Debug labs, the scripted twin of each interactive lab key**
`--debug-livery` · `--debug-mesh` · `--debug-select` · `--debug-nodelab` · `--debug-damage` · `--debug-names` · `--debug-fps` · `--debug-join` · `--debug-waves` · `--debug-wingmen` · `--debug-preset` · `--debug-pointer` · `--debug-scoreboard` · `--debug-wash` · `--debug-anim-ui` · `--debug-collision` · `--debug-dzpaths` · `--debug-ainets` · `--debug-targets` · `--debug-markers` · `--debug-spectate` · `--debug-pause` · `--debug-objective` · `--collision` · `--debug-colliders` · `--debug-classoverlay` · `--debug-tilegrid` · `--debug-clutterflag` · `--markers`

**Dumps and the test harness, report text plus a verdict, then quit**
`--dump-markers` · `--dump-weapons` · `--dump-loadout` · `--dump-flight` · `--dump-config` · `--dump-mips` · `--dump-ai` · `--dump-tilegrid` · `--run-tests` · `--damage-test` · `--effects-test` · `--hud-font-test`

**Logging and profiling**
`--log` · `--perf` · `--gc-types` · `--debug-anim` · `--anim-lod` · `--hitch-inject`

**Rendering probes, is this thing drawing at all?**
`--tex-override` · `--tex-census` · `--no-fog` · `--no-flare` · `--no-clutter` · `--clutter-templates` · `--no-zone-cull` · `--sky-zone` · `--mips` · `--no-cockpit-pass` · `--graphics`

**Map-edge continuation, the fold past the map boundary**
`--map-edge-block` · `--map-edge-mode`

**Scripted input, fly a fixed stick input, no hands**
`--hold` · `--yaw` · `--pitch` · `--look`

**Data paths, override where the extraction is read from**
`--data-root` · `--gamez` · `--textures` · `--zrdr` · `--interp` · `--sounds` · `--zip-assets` · `--messages`

**Audio**
`--mute` · `--volume`

Neither of those reaches the player's own mix, and no flag or config key does. The four volume
levels (Master, Music, Effects, Voice) are set on Preferences' AUDIO page, saved in
`user://options.json` and written on the three category buses beneath Master, so the two gains
multiply rather than compete: `--volume=0` silences a repo run whatever the saved levels say, and a
`--det` run reads none of them. The mix is
[`docs/menu-presentations.md`](menu-presentations.md)'s Audio section.

Three flag families live in their own sections further down, with the lab they script:
the shared selection (`--debug-select`), the node lab (`--debug-nodelab`), the world damage lab
(`--debug-damage`).

⚠ **A new flag adds its index entry and its `## Flags` bullet in the same edit.** Both are in this
  file so they cannot drift apart. `PROJECT_CONTEXT.md` carries a curated day-to-day *subset*
  whose rows are glosses, never the description of record, a behaviour change edits the bullet
  here.

**Written exceptions to "one flag, one bullet":** `--spawn-dir` shares `--spawn-at`'s bullet, since
the pair is one mechanism, so `Grep` the partner's name to find it; `--debug-nodelab` and
`--debug-damage` each carry a short opener bullet plus the full behaviour under their lab's own
section further down. The counts reconcile as **151 index entries, 151 parser flags and 152 bullet
lines**.

## Flags
- `--viewer` (the static inspection view: the parked-plane orbit, or with `--chapter=` the static world for deterministic weather, fog and edge screenshots. It hosts three labs, always built and needing no extra flag: the damage lab on **F19**, the livery lab on **L** and the mesh lab on **M**. All three start hidden, so an unadorned `--viewer` screenshot matches the pre-paint viewer. F19 toggles the damage lab as a whole, slider panel and HUD gauges together; the panel's own checkbox controls the gauges while it is up. **P** halts the simulation and **`.`** advances one frame)
- `--debug-livery[=N]` (`--viewer` only: open the livery panel at launch and, with N, step the pattern N times first, one scripted screenshot then exercises the stepper + repaint + widget sync, not just the layout; same role as `--debug-scoreboard`/`--debug-join`)
- `--debug-mesh[=spec]` (open the **mesh lab** at launch with its modes preset. The subject is the parked plane in `--viewer` and the run's selection elsewhere, where it stands in for the M press; the panel is hidden in a `--screenshot` run. Comma-separated tokens, unknown ones reported: `normals[=corners]`, `color=provenance|direction|winding`, `wire[=seams|seamsonly]`, `boxes`, `enginewire`, `cull=double|single|inverted`, `source=flat|smooth|negated`, `headlight`, `ambient=off`, `sun=<energy>`, `dir=x/y/z` (slashes; the direction the light travels), `cycle=N`, `force`, `restore`)
- `--collision[=show]` (**build the world's colliders in a mode that otherwise builds none**: `--freecam`, `--anim-lab` and `--viewer` build no `StaticBody3D` at all, so what is solid there is unmeasurable without it (WORLD-9). `=show` also opens the **C** collider-wireframe overlay, colouring each wireframe by the surface id its body resolves to. The id is per body, not per polygon: the colour is what the engine selects. It prints per-id counts and the on and off transitions separately (WORLD-10). In `--viewer` it builds colliders but binds no overlay (C is the mesh lab's cull cycler))
- `--debug-colliders` (open the **C** collider-wireframe overlay **without** forcing the collision
  build first; the opener that always builds is `--collision=show`. This is how the "no collision
  here" notice (WORLD-9) is exercised in `--freecam`/`--anim-lab`, which build none on their own;
  pass `--collision` alongside it to populate the overlay there. In a flight mode, or after
  `--collision`, the colliders already exist and the overlay just opens against them. Distinct from
  `--debug-collision`, which draws the flown plane's own probe, not the world's)
- `--debug-classoverlay` (open the **H** colour-by-class overlay at launch, in
  `--freecam`/`--anim-lab`/`--fly`/`--stunt`. It tints every world mesh by what it is rather than what it looks like: destructible red, from the `DestructibleRegistry.Resolve` climb a weapon hit takes, so a door that only looks breakable
  reads as scenery; facade pink; clutter green;
  everything else scenery blue. It is rebuilt on every H press, so a death never tints a freed node, and it is not keyed on `SceneBuilder.SurfaceMeta`, which answers what a bullet does here. Debug-only: goldens are unaffected)
- `--debug-tilegrid` (open the map-edge tile-grid overlay at launch. Flag-only, and built wherever a map-edge continuation exists (`--fly`, `--freecam`, a `--sky-zone` viewer). It
  tints every ground tile at 20 % by how it was folded, neutral in-map and one hue per fold parity
  (`copied`, `x-mirrored`, `z-mirrored`, `xz-mirrored`); one colour band is one block depth. Ground only. Pair it with `--no-fog` (SHOT-14). It paints only the tiles the extender accepted, so refused
  ground shows as unpainted terrain; `--dump-tilegrid` names the node and the reason)
- `--debug-clutterflag` (build the chapter world recoloured by each polygon's decoded `no_clutter`
  flag. A polygon carrying the flag is RED, one without it GREEN, and all clutter BLUE. A decoration's own polygons are unflagged, so
  without it a whole city block reads as clutter-eligible ground. The flag is polygon bit `0x800`, `GameZPolygon.NoClutter` (`docs/formats/gamez.md`). There is no runtime toggle: the recolour is written into the vertex colours at build time. One log line
  per build carries the census, which counts polygons per built model, not per placement)
- `--dump-tilegrid[=path]` (build the chapter world, write the **map-edge tile census** and quit;
  the written twin of `--debug-tilegrid`, and the only place a refused tile appears. JSON to
  `./.scratch/tilegrid_<chapter>.json` unless a path is given. One row per tile candidate: verdict
  (`Accepted` / `TooSmall` / `TooLarge` / `SkyOrCloud`), footprint in metres and in cells, its grid
  cell and its surface class, plus a per-cell roll-up carrying `coverX`/`coverZ` and a `suspect`
  flag for a border cell short of ground. Implies `--freecam`, where the extender it reports on is
  built)
- `--map-edge-block=N` (how many border cells deep the repeated block past the map edge is.
  **Default is per chapter** (`MapEdgeExtender.DefaultBlockCells`): **2** on C1/C2/C4, **1** on C5
  and on C1B/C1C/C2B/C3, whose borders carry only water tiles, which fixes them at 1 and makes the
  depth moot there. An unknown chapter falls back to 1. Clamped to the chapter's grid, at which
  point the continuation repeats the whole map. ⚠ **Do not size this from a video-derived period**
  (`docs/verification.md` DET-12))
- `--map-edge-mode=mirror|repeat` (**default `repeat`: translate the block, which is what the
  original does**, matching it on C1/C2/C4/C5 with no seam gaps. `mirror` alternately reflects the
  block instead, so every seam is a shared mirror plane, and is kept for comparison. No key toggles
  it. ⚠ Do not read the choice off a frame-derived strip period (`docs/verification.md`
  DET-12). Repetition that shares the map-edge vertex row is deliberately not offered: it is mesh
  surgery rather than a per-cell transform, and plain repeat shows no step at the shipped block
  depths)
- `--plane=` (a comma-separated list gives one plane per splitscreen player, `--plane=player_bhawk,player_fury`, and implies that player count unless `--players=` says otherwise)
- `--rof=` (default `extracted/rof`, the extracted UI archive holding the paint patterns; run `ExtractRof.ps1` to produce it)
- `--paint=<pattern|random|none>` (aircraft livery: a pattern name, `random`, or `none`. **Patterns are per aircraft**, and naming one the plane lacks logs its actual set and paints decals only; `random` draws from that plane's set. Comma-separated per player like `--plane=`, the last covering the rest. **The default everywhere is `player_fortune`**, the only pattern covering all eleven airframes; `--paint=none` asks for the bare shipped skins. Instant Action's enemies keep their own militia skins rather than the player militia's colours)
- `--paint-color=R,G,B/R,G,B/R,G,B` (override the scheme's three colours, slot 1 body, 2 dark trim, 3 light trim; integer 0–255, fewer than three repeats the last)
- `--paint-decal=nose,tail,wing` (override the three decal indices into the chapter archive's 00–49 set: 00–20 squadron logos, 21–49 nose art)
- `--paint-seed=N` (seed the random-livery RNG directly, overriding the `paint` stream `--seed`/`--det` would derive, pins liveries alone in an otherwise unpinned run)
- `--damage[=part:frac,...]` (**decide whether the damage lab opens at launch**; every session with an aircraft builds one, reachable on **F19**. On its own it implies `--viewer` and drives the parked plane, whose per-part HP sliders play the torn-skin flips, panel fires and nose smoke. With an explicit `--fly`/`--stunt` it stays in flight, where the sliders write the aircraft's real per-part HP. Presets take a fraction of 0-1 or a percent above 1 (`--damage=leftwing:0.25,nose:40`). Ignored by `--freecam`/`--anim-lab`, which have their own world damage lab. Splitscreen: P1-only)
- `--chapter[=C1]` (which chapter world to build, flown by default, or rendered statically with `--viewer`; takes `C1`/`C1B`/`C1C`/`C2`/`C2B`/`C3`/`C4`/`C5`, drives the default gamez to `<chapter>/gamez.zip` and textures to `<chapter>/texture.zip`; the world node name is always `world1`)
- `--stage=empty` (**replace the chapter world with a synthetic test stage**: a flat, collidable 20 km ground plane under a code-drawn 100 m grid, the default sky and sun, with the plane over the grid origin. No chapter gamez, mission setup, clutter, animation program, weather, skydome or edge extender is built unless `--zep=` grafts one in, leaving the aircraft, its HUD, flight, collision and both weapon paths. With no chapter gamez, rockets fly without their FLYOUT body, pylons carry no mounted ordnance, and an impact draws the spark fallback. An unknown stage name is reported, not guessed)
- `--node=<cs_name>` (in `--viewer`, the default for this flag, or `--anim-lab`, **build only the named gamez subtree** out of `--chapter=`'s world, with the camera auto-framed on it. Matching is on the source `cs_name` meta, never the sanitized Godot node name. Multiple matches are normal: the first is built, and the ambiguity is a warning. A name matching nothing lists the candidates containing it and quits cleanly (exit 0). The mission's interp script and the clutter are skipped, but the animation program still loads and binds. `--pos`/`--direction`/`--lookat` override the framing)
- `--fly` (**the default**: free flight, a chapter world with skydome and plane, on the original's own keymap (`docs/controls.md`): the arrows pitch and roll, `,`/`.` rudder, `=`/`-` throttle and the digits set it outright, Backspace respawns, **Esc** (or gamepad Start) halts the sim and opens the pause board's Resume, Restart and Exit menu, where the chase camera free-orbits the frozen plane, and **`.`** advances one sim frame while halted. It defaults to C1; combine it with `--chapter=` for another. It spawns from the mission's `ia.json`, so see `--mission`/`--scenario`/`--spawn`)
- `--stunt` (`--fly` plus the mission's Danger Zones (`ia.json` `dzones`) as fly-through objectives, spawning from the `stunt_flying` list, which it forces unless `--scenario=` is explicit. The objective marker HUD draws the marker, edge arrow and bearing with the run timer; completing every zone raises the results scoreboard, which halts the sim (**R** restarts). CLI only: the launchscreen's stunt row attaches an Instant Action mission instead. With `--players=N` it is a race, each pilot flying their own zones and clock, ending on a ranked board; race totals are not persisted)
- `--vs` (**"Dogfight" mode**: splitscreen free-for-all deathmatch, `--fly` plus the `dogfight_ace`
  spawn list unless `--scenario=` is explicit. It **beats `--stunt` by fixed precedence** rather
  than last-flag-wins, dropping stunt mode with a warning when both are given.
  `--vs-kills=`/`--vs-time=` set the match rules. The menu requires two joined pilots before it
  starts a match and this flag does not, so `--vs --players=1` parses and runs with a warning. The
  match bookkeeping, HUD and win/lose flow are not built, so the session flies the `dogfight_ace`
  spawns with nothing scoring)
- `--vs-kills=N` (with `--vs`, the kill target that ends a match early once a player reaches it.
  Default 5; `0` disables the kill limit, so the match runs to `--vs-time=` alone)
- `--vs-time=N` (with `--vs`, the match time limit **in minutes**. Default 5; `0` disables the time
  limit, so the match runs to `--vs-kills=` alone. Both limits at `0` is a match with no end
  condition, accepted, not guarded against, since the mode's win/lose flow is a later item)
- `--coop` (plain splitscreen free flight defaults to **FFA**, every
  human on their own team (`AimAssist.TeamOfPilot`), so aim assist, world turrets and AI gunners
  treat the other humans as hostile. `--coop` opts a plain `--fly`/`--stunt` session into one
  shared human team instead, the same team Instant Action puts its wingmen on
  (`AimAssist.PlayerTeam`). Has no effect with `--vs`, Dogfight's FFA is explicit and outranks it,
  logged as a warning if both are given)
- `--freecam` (**spectator mode**: the live chapter world with **no aircraft at all**, seen from a free-flying camera. RMB looks, WASD and Q/E move, Shift and Ctrl scale the speed, the wheel sets the base. There is no collision. It starts at the mission's spawn position, which `--pos=`/`--direction=` override. **P** halts the simulation and **`.`** advances one frame. Left-click selects, **PgUp**/**PgDn** walk the ancestor ladder, **M** opens the mesh lab and **C** the collider wireframes. Wins over `--fly`/`--stunt`/`--viewer`/`--damage`)
- `--anim-lab` (**the animation debugger**: the chapter world as a quiet stage (no ON_STARTUP defs, no startanims) under a deterministic fixed-dt clock. The camera is the freecam, and clicking an object locks the camera onto it. Transport is an on-screen panel with **P** pause, **`.`** step, **R** restart and **F** the def picker; **M** and **C** work here too. The picker lists every def; the timeline draws authored start times against the ticks the runtime fired. The lab UI is hidden in a `--screenshot` run unless `--debug-anim-ui` forces it. Wins over the other modes)
- `--play-anim=<name>` (implies `--anim-lab`: resolve the def(s) carrying that ANIMATION_NAME and play them at launch, exactly the way bootstrap pass 3 starts a startanim, anchored defs once per anchor, unanchored ones globally resolved, and auto-frame the orbit camera on the played def's anchor (fallback: its first resolved target node; `--pos`/`--direction` suppress the auto-frame). An unknown name logs and leaves the stage quiet)
- `--seed=N` (**the session's master seed**: every random draw comes from a named subsystem stream derived from it as `splitmix64(master ^ fnv1a(name))`. **Default 1 under `--det`, `--anim-lab` and `--effects-test`, drawn from the clock otherwise**, so a bare `--fly` gets a random spawn, liveries and spread; the resolved value prints as `rng: master seed N`, and passing it back replays the run. An unpinned session advances its master per flight, so flying again is a new draw. Weather zones, wave composition and terrain placement do not vary. `--paint-seed=N` overrides the paint stream alone)
- `--debug-anim-ui` (implies `--anim-lab`: force the lab's def picker + timeline visible in a scripted `--screenshot`, the timeline-verification path, since the whole lab UI is otherwise hidden there to keep shots byte-identical; the same house convention as `--debug-livery`/`--debug-scoreboard`)
- `--det` (**the determinism bundle**: a fixed-dt sim clock, master seed 1, `--spawn=0`, pinned liveries, `--no-pads` and `--jitter=0`. The clock steps 1/60 s per rendered frame, so the sim runs at the refresh over 60: a 120 Hz screen doubles a cheap stage. Shader time is clock-driven too, so pixel output is a function of frame count; `config.json` overrides and every saved option are dropped (DET-8), so no display setting reaches a golden. Each pin is overridable; `[core] det ... via=` names what won. `--screenshot=`, `--dump-*` and the `--*-test` probes imply it; `--no-det` opts out)
- `--no-det` (**opt out of the implied bundle**, the one way to ask for wall-clock behaviour whatever else is on the command line: it beats both the implication (`--screenshot`, the `--dump-*` reports, `--damage-test`) and an explicit `--det`, so `--det --no-det` runs on the wall clock. Use it to measure what `--det` hides: frame-rate-dependent behaviour, live randomness, the same-build noise floor. It announces itself as `[core] no-det: --screenshot would run deterministically ...` and prints nothing at all when there was no implication to cancel)
- `--no-pads` (ignore every gamepad, debug/verification aid: a connected pad with stick drift steers the free camera and nudges the flight model, quietly making a scripted screenshot non-deterministic; SDL's own hints don't disable pads in Godot 4.7, so this is our own switch. **Part of the `--det` bundle**, so a scripted run already has it)
- `--perf` (every 60 frames: `[perf] window sim_frame= frames= wall_ms= fps= frame_ms= script_ms= proc_ms= proc_max_ms= proc_passes= ai_ms= ai_planes= render_cpu_ms= gpu_ms= physics_ms= phys_tick_ms= phys_tick_max_ms= phys_hz= draws= prims= nodes= mem_mb= max_ms= p95_ms= sim_ms= proc_sites_ms=`. Means but for `*_max_ms`/counts; `phys_*` empty under `--det`. **Read `proc_ms`, `ai_ms`, `render_cpu_ms`, `gpu_ms`, counts**, not `fps`/`script_ms`/`physics_ms` (PERF-1, PERF-2); `sim_ms` splits the tick by phase, `proc_sites_ms` the pass by consumer, each slot `mean/max`. Also `[perf] startup`)
- `--perf`'s **GC readout**, one `[perf] gc up_s= win_s= pause_ms= pause_per_s_ms= gc0= gc1= gc2= fin= fin_per_s= alloc_mb_s= heap_mb=` line per ten wall seconds (`GcTrace`). `fin` is how many FINALIZABLE objects died, which is what the pause tracks; `up_s` is process uptime, so drop the windows under about 50 s, which are the world build settling. **Judge a change on `fin_per_s` and `pause_per_s_ms`**, never on the per-collection pause (PERF-19, PERF-20)
- `--gc-types` (implies `--perf`; adds one `[perf] gc-types win_s= samples= per_s= top=Type:count,...` line per GC window, the types the runtime's allocation sampler saw most. One sample per ~100 KB allocated, so the tally is a share of BYTES: a small wrapper type high on the list is made in very large numbers, which is how `fin_per_s` is traced to the call making the wrappers. The listener's own event objects (`MoreEventInfo`, `EventWrittenEventArgs`, boxed payload) head every list and inflate `alloc_mb_s`, so read past them and never judge allocation on such a run)
- `--no-vsync` (**a measurement flag, not a display one**: it uncaps the frame loop (vsync disabled,
  `Engine.MaxFps` 0) so `--perf`'s frame timings stop sitting pinned at the refresh rate. Safe with
  `--det`: that clock advances one sim step per *rendered* frame, so the simulation is identical and
  only the wall time changes. Precedence: **this flag**, then the saved `vsync` option (the VIDEO
  page's row, which can also name a frame cap), then the `display.vsync` config key (`true` turns
  it on), then off, uncapped. `--det` reads no saved option; the `[perf] vsync` line names the winner)
- `--debug-anim` (log every live animation motion's target node, world position **and rotation** once a second, rotation because a spinning prop (`OBJECT_MOTION`) turns in place and a position-only line reads identically whether or not it runs. It also prints each `If`/`Elseif` condition on its first evaluation and thereafter only when its verdict flips, and every ambient `SOUND_NODE` emitter's host, position, distance, range and playing state. The `dist` column is the range to the nearest of the session's audio listeners, one per pane, and names it; that is a range, not a loudness)
- `--log=<spec>` (**console log filter**: a comma list of `cat`, `cat:level`, `*`, `*:level`, or a bare `level`, over the nine categories `anim world flight weapons sound perf test ui core` and the four levels `error warn info debug`. A bare category means debug, a bare level sets every category; the default is `info`, and **warnings and errors are never suppressible**. **It moves the console threshold only: every `Log` line at every level also goes to `.scratch/logs/<mode>-<stamp>.log`** (`logs\` in an exported build), line-flushed. `--debug-anim` implies `--log=anim:debug,sound:debug`)
- `--anim-lod=N` (our answer to the data's `ANIMATION_LOD` condition, a **quality setting**, not a fact about the world. Default 2 = the reader's `HIGH`, the only tier anything in this install asks for, so every LOD-gated branch runs (the refinery/dock/lighthouse light sequences, the muzzle bursts, the wing-light blinks). Lower it purely to A/B what the original hid on slow hardware)
- `--hitch-inject=[alloc:]<ms>[@frame]` (inject a synthetic stall of known magnitude, so `HitchMonitor` has something deterministic to verify against. It fires once, on the stated frame in `HitchMonitor.FrameCount`'s own space, never the sim frame, since the injector has to work with no session built at all; a bare `<ms>` fires at frame 300. The default form busy-waits; the `alloc:` prefix burns the same wall time allocating and discarding 4 KB buffers, the only way to move the GC columns on demand. **The frame must clear the grace window in wall time, not frame count** (PERF-12))
- `--debug-dzpaths` (build the danger-zone route/gate geometry the world skips; debug inspection only, never in the original. The material-matched gate pair is green then red at 50% transparency; the route is an open white line strip)
- `--debug-ainets[=name,...]` (open the AI patrol-net overlay at launch: the chapter's `neindex` waypoint graphs ([`docs/formats/ai-nets.md`](formats/ai-nets.md)), with edges drawn as the explicit edge list and never as an assumed loop, since the graph branches. While it is up it also draws one live leash per AI aircraft, from the plane to its follower's `CurrentTarget`, so a follower that has stopped advancing shows as a leash that stops changing. Bare draws every net, a comma-separated value only those names. **F13** toggles it in any chapter world)
- `--debug-scoreboard` (with `--stunt`, force-complete the run on the first frame with synthetic splits so the results scoreboard renders immediately, for a deterministic `--screenshot`. In a splitscreen race the players finish staggered by index, so an early `--frames` catches the waiting banner and a later one the shared board. With `--vs` it fires one scripted, attributed kill on the first sim step, P1 downing P2 through the real `Crash`/`Downed` path rather than a synthetic score, so the shot has a real K/D and kill line; with `--vs-kills=1` that kill also completes the match)
- `--debug-pause[=frame]` (open the pause board at that sim frame, the scripted twin of the Start press, so a `--screenshot` captures the pause screen with nobody at the controls; bare is frame 1. Player 0 owns the pause, as a solo press would. `--frames=` counts rendered frames and the board halts only the sim, so pass a `--frames=` comfortably past the pause frame for the shot to land. A campaign session draws its objectives readout there, so this is how that readout is captured, and a frame late enough for the mission to have run is what lets the shot show a completion the sim has reached)
- `--debug-objective=N` (campaign only: wake objective N and drive every node its `INACTIVEn` conditions name inactive, at session build. It is the scripted twin of flying whatever completes that objective: the graph then completes it off its own conditions on the first step, so the readout's mark comes from the real completion path rather than being faked into the display. The build summary says `forced OBJECTIVEN`, or `(not armed here)` when the mission arms no such number. Pair it with `--debug-pause=` for a shot of a marked objectives line)
- `--debug-wash=N` (address two scripted victim-routed screen washes to viewer N, 1-based, through `ScreenFlash.PlayBlend`: red at weight 1 for 5 s on the first sim frame, then white at weight 0.5 for 5 s two seconds in, so the second lands on the first and the blend is on screen as well as the routing. In `--coop --players=2`, `--debug-wash=2` washes pane 2 and leaves pane 1 untouched; an N no pane answers to paints nothing. It fires on sim time in every clock mode, so a `--det --screenshot` at a chosen frame catches the attack, the sustain or the release.)
- `--debug-markers` (mark **every live aircraft at once** on the targeting HUD, alongside your own selected target's marker: red for a hostile team, blue for your own, each carrying the full identity string (`TargetHud.DebugTag`): type, slant range, health and armour as separate whole percentages, and the AI mode. Health, armour and mode are omitted rather than defaulted when the source carries no figure. It reads the projectile pool's live-aircraft roster, so a spawn appears and a downed plane drops. Your own aircraft is never marked. **F16** toggles it in flight, on every pane at once.)
- `--debug-spectate` (build the session exactly as it would be flown, then take **every human out of it**: each player's aircraft is pinned where it spawned and goes inert (undrawn, uncollidable and not a candidate in any AI scan, so nothing pursues you), and the pane's camera becomes the `SpectatorCamera`, parked at the spawn and following the first AI aircraft until a move input releases it. It runs after the rigs are built, so spawn placement still reads the player's real position. ⚠ The mission still runs its own end conditions, so one whose enemies have nobody to shoot never resolves)
- `--debug-targets` (switch the **targeting overlay** on at launch: one line from every turret and AI gunner to the target it has actually acquired, read off that gunner's own live state and never re-derived. Red is firing this tick, amber tracking with a gate that will pass on its own, grey held by a gate that will not. The HUD counts the four states, then per shooter its gate, what it is aiming at and the range, aircraft before emplacements and a count of any left out. Depth test is off, since the interesting line runs into a hull. The sight lines draw in every pane, the roll-call once)
- `--debug-names[=meshes|all]` (switch the **node-name labels** on at launch, available in both `--viewer` and flight and reachable this way alone, since the labels carry no key; `meshes` (default) labels only nodes that draw something, `all` includes the empty group/pivot nodes. **Splitscreen:** the nearest/de-clutter selection reads P1's camera alone, the labels it picks are P1's view of the world, but the label nodes themselves are world-space and render in every pane)
- `--debug-fps[=compact|full]` (switch the **frame-cost readout** on at launch, in every mode including the launchscreen: frames per second, the current frame's wall cost and the worst recent frame, a peak that spikes and decays over a few seconds so a hitch you felt is still readable a moment later. `compact`, the default, is that trio; `full` parses and cycles but renders the same panel. It is fed the same raw `Stopwatch` cost `HitchMonitor` ticks on, never Godot's `delta`. Off by default, and drawn once for the window rather than per splitscreen pane, so the goldens are unaffected)
- `--debug-select[=x,y[,up]]` (`--freecam`/`--anim-lab` only, the **scripted twin of clicking an object and pressing PgUp**. On the first frame it casts the same pick ray a click would, at screen position `x,y` (default the middle of the viewport), then walks `up` rungs toward the world root. In any other mode it warns and is ignored. The report goes to the `ui` log category and is the whole ladder, one `select rung=...` line per rung with the current one starred. **Pin the camera with `--pos`/`--direction` first**: a screen-position pick is meaningless without the pose it was taken at)
- `--debug-nodelab[=spec]` (`--freecam`/`--anim-lab` only, open the **node lab** (N) at launch and dump its readouts to the `ui` log category. Tokens: `deps`, `dest`, `open`, `node=<cs_name>`; full behaviour under "The node lab" below)
- `--debug-damage[=script]` (`--freecam`/`--anim-lab` only, open the **world damage lab** (F19) at launch and run an ordered damage script against the selected destructible, logging each step to the `ui` log category. Steps: `node=<cs_name>`, `pool=<n>`, `hp=<value>`, `kill`, `reset`, `tick=<seconds>`, `open`. Forces the collision build on so the kill's collider census is real; full behaviour under "The world damage lab" below)
- `--markers` (**implies `--viewer`**: open the **marker overlay** at launch, the firepoint, pylon and target gizmos on the parked aircraft. Firepoints draw orange, firepoints sharing a mount magenta, pylons cyan and the aim `target` green. Every gizmo dot shows; the labels de-clutter nearest-first. Toggle it in any `--viewer --plane` session with **K**, or combine it with `--screenshot`. Only the parked plane carries a marker rig, so a `--viewer --chapter` world has none, and `--viewer` forces `--players=1`, so the overlay is not reachable in splitscreen)
- `--dump-markers[=plane]` (implies `--det`; print each player airframe's marker rig to stdout and `./.scratch/markers_dump.txt`, then quit. It needs no world or camera, so `--headless` makes it windowless. Each block lists every `firepoint*`/`pylon*`/`target` with its plane-frame position (nose −Z, right +X, up +Y), a firepoint's mirror pair and any co-located mounts. The `docs/formats/markers.md` tables regenerate from it, and the `markers-rig` suite asserts the same report. The optional value filters to one airframe by model node or display name; an unmatched value lists the names)
- `--dump-weapons[=id|name]` (implies `--det`; print all 48 `BALLISTICS` defs of `weapons.json` (`WeaponDefs`) to stdout and `./.scratch/weapons_dump.txt`, then quit. Each block gives the id, `NAME`, the `DESC` display name resolved through `messages.json`, the ballistics, damage and allotment numbers, the class flags and the `FIRE`/`FLYOUT`/`IMPACT` bindings. A clean run reports **no unhandled keys**, the tripwire for data growing a key the reader has not learned; `--run-tests=weapons-defs` asserts it and the 48 count. Optional value filters by id or `NAME` substring)
- `--dump-loadout[=plane]` (implies `--det`; for each plane in `stock_loadouts.json`, **build its model and bind the stock loadout** (`Loadout`), reporting the resolved gun groups, the inert turret slots and the hardpoints (count × per-pylon `CLUSTER_SIZE` = total), to stdout and `./.scratch/loadout_dump.txt`, then quit. A clean run binds all 11 with **every marker resolved**; a marker that does not resolve prints a loud `!!` line naming plane, slot and marker rather than being skipped. `--run-tests=loadout-bind` asserts the same verdict. Optional value filters by def or model substring)
- `--loadout=<def>` (bind the named loadout def instead of the plane's own, a testing override. In flight the flown plane carries that def's guns and hardpoints, whose markers must resolve on the flown model. **It takes a menu-chosen fit away entirely** rather than merging with it, so the flag names the fit outright. With `--dump-loadout` it binds every dumped plane's model to that def, which is how the missing-marker error is exercised: `--dump-loadout=Kestrel --loadout=pbloodhawk` binds a loadout wanting `firepoint8` to an airframe with seven firepoints and prints the loud error)
- `--dump-flight[=plane|=all]` (implies `--det`; step a throwaway `FlightModel`, needing no world or scene, through sixteen manoeuvres and print each one's number beside the value it is judged against, to stdout and `./.scratch/flight_dump.txt`, then quit. ⚠ **The `target` column is never footage.** Five rows carry a decoded target and the other eleven show `(not asserted)`; a filmed figure disagreeing with a traced mechanism is named in the row as **discarded** and gates nothing (`docs/org/flightModel.md`, "Parity ledger"). **`--dump-flight=all` flies all eleven stock airframes in one run**)
- `--dump-config` (implies `--det`; exercise the Config-wired modules once (`Config`, `src/Utils/Config.cs`), then write a fully-populated template of every tunable key with its in-code default, nested by module block, to `./.scratch/config.dump.json`, and quit. Copy that to `CSVM/config.json` (git-ignored) and edit only the values you want to override; every other key falls through to its `const`. A key in the file matching no tunable is warned about loudly, as is a queried key missing from a loaded file. Deleting the file restores pure-default behaviour)
- `--dump-mips[=name]` (implies `--det`; for every base texture in the chapter's archive shipping an authored `_1`/`_2` sibling, build the chain through `TextureArchive.BuildMipped` and print each level's size, mean luminance and share of pixels above luminance 128, beside the authored artwork's own numbers and whether the installed level **is** that artwork. The header names the chapter's authored mip LOD bias and the texel density each level is first read at under it. Writes to stdout and `./.scratch/mips_dump.txt`, then quits. **Run it once per `--mips=` policy**)
- `--dump-ai[=chapter]` (implies `--det`; report five AI data families read off the extraction
  (`Probes.Ai`, no world and no scene): patrol nets, `aiv` vehicle rosters, `ai.zrd` turrets,
  zeppelins and generators (`docs/formats/ai-nets.md` and its neighbours). With no value it walks
  every chapter and mission directory under the data root; a chapter value restricts the nets, `aiv`, zeppelin
  and generator half, while `ai.zrd`'s turrets are one shared file always reported in full. Writes
  to `./.scratch/ai_dump.txt`, then quits, exiting nonzero on a parse failure or an unknown
  chapter)
- `--fire` (**hold the gun trigger down** for the flown plane(s), the scripted-run equivalent of holding Space, as `--hold` is for flight input. Combine with `--hold`/`--screenshot` for deterministic firing captures; every player fires. In interactive flight the trigger is **Space** or **gamepad B**)
- `--fire-rockets` (**hold the rocket trigger down** for the flown plane(s), auto-repeats a launch at the rocket's `FIRE_RATE` cooldown (1 s), unlike a human pull which is one rocket per press. In interactive flight the rocket trigger is **F** or **gamepad A**; one rocket per pull, drawn from the next armed pylon round-robin)
- `--gun-select=N` (**initial gun group**, 0-based; `0` = the first group, the default. **Only one gun group fires at a time**, a headless testing hook so a scripted run fires one group in isolation. Interactively the gun selector cycles with **F3**/**F4** or **gamepad D-pad Right** (tapped forward, held back), the hardpoint/ordnance selector with **F5**/**F6** or **gamepad D-pad Left**)
- `--target=<what>` (**the initial target selection**: `nearest`, `crosshair`, `next`, `none`, or a name to pin. The flag reaches only a state the pilot could reach: `nearest` is the head of the cycle, not the nearest thing in space; `crosshair` is the 15-degree nose-cone scan and reaches allies; `none` stays cleared. A name matches pool entries case-insensitively, a plane's node name (`ai1_player_fury`) or a zeppelin sub-part's; no match warns with what is selectable. Applied once per pane, on the first frame its pool is filled, then spent. Keys **T**/**Y**/**U**/**I**/**O**)
- `--incoming[=metres[,wep_id]]` (**the incoming-fire test rig**: a phantom shooter 120 m on every player's six walks a burst up the tailpipe, four rounds a second, under a shooter identity no player holds, so both cues are reachable in single player. It fires the target's own gun, or the named weapon. The first rounds are absorbed by the shipped `warning_shot_*` shield and sound `bullet_warning_sg`; two seconds of them saturate it, and rounds then damage and ring `bullet_hit_sg`. The metres offset the burst sideways, alternating sides (default 0, on the airframe); a wide offset hits nothing)
- `--ai=<plane>[:<net>][:accent=<id>][:def=<vehicle def>][:team=<id>][:n=<count>][:pos=x/y/z][,...]` (**spawn AI-piloted aircraft**. `n=` makes an entry a squadron; `team=` is the only way two share a side, since a plane naming none is banded alone and fights every other. Netless it sits 250 m ahead of P1, fanned 60 m; under `--stage=empty` each side anchors to a 1000 m ring about the origin, `pos=` overriding. `<net>` is a chapter `neindex` net, or `grid`, the stage's own ring; either patrols on its volumes. `accent=` a roster `accentID`, `def=` a militia variant. Arm with `--ai-attack`)
- `--ai-attack[=<1-9>]` (**arm every AI plane this session spawns**, from `--ai=` and `--generators`, with forward-gun gunnery. Bare, each plane flies its vehicle def's nine-slot pilot vector, falling back to 5 for an unset slot; `=N` pins every plane and slot to N. The rating interpolates the dead-eye and quick-draw cones from the shipped `ai_skill_parameters`. The gunner auto-targets the nearest hostile aircraft and fires through `FireControl` only in pursue, and only when the intercept is inside the weapon's RANGE and both cones. An AI never gets the human aim assist)
- `--ai-damage=<fraction>` (**spend every `--ai=` plane's hull down to this health fraction as it spawns**, so a run sees an enemy already staging its `injure_anims` ladder. `--damage=` is the player's counterpart and writes per-part HP, which an AI airframe resolves none of; this spends the whole-vehicle pair the ladder reads, armour first and health second. Clamped to [0.01, 1] and never zero, since a preset that killed would leave a wreck, and logged as `ai damage preset:`. Only `--ai=` planes take it. A frame hash cannot see it (SHOT-29): read the `damage stage:` lines)
- `--ai-targeting=<aircraft-first|decoded>` (**what an ally's AI fights**: the default `aircraft-first` ranks live enemy aircraft ahead of every turret and structure candidate, in the aeroplane picker and the turret gunner alike, so wingmen and an airship's guns agree; a structure is fought only when no aeroplane is in reach. `decoded` restores the original's one running minimum over all four pools, where a def's `target_bias` and `struct_bias` (negative against a minimised rank, so both attract) rank a structure about 100 m ahead of an equidistant aeroplane. An unknown word keeps the default)
- `--difficulty=<normal|hard|hardest>` (**the difficulty setting**: enemy armour and health maxima scale at spawn by 0.75, 1.0 or 1.25, nothing else (`docs/org/vehicleDamage.md`); wingmen are spared and an Instant Action wave's `enemy_skill` overrides it per spawn. Also takes `novice`/`veteran`/`ace` and `0`/`1`/`2`; an unparseable name counts as absent. The Options screens save a `difficulty` word into `options.json`, read at every launch: this flag beats the saved word, which beats the default `normal`; `--det` reads no saved option)
- `--no-assist` (**disable the rubber-band assist**: every AI mode machine this session spawns gets `AssistEnabled` false, so `lay off` is never entered and an engaged AI stays in pursue. The assist is on by default, the original's shipped behaviour: an AI whose human target is chasing it from more than 350 m back eases into `lay off`, holds its course, walks its throttle toward `sixth_sense_factor` times the pursuer's speed and holds fire, until the pursuer closes inside 250 m or stops chasing. Meaningful only alongside `--ai-attack`, since without a gunner no AI acquires a target)
- `--generators[=<plane>]` (**run the mission's egen enemy generators**: load `egen.zrd.json` for the flown mission, drop a generator whose host node or nets do not resolve, then step each survivor's cycle: waves of `wave_size`, individuals `ind_period` apart, waves `ind_period + wave_period` apart, capped at `max_active` live spawns. The optional value picks the airframe for a generator that authors none (default `player_bhawk`). The authored `capacity` is never read; a generator launches only what a `WAKEUP_GENERATOR`, a wave or a cutscene credits, which `--wake-generators` grants at build)
- `--zeppelins` (**place and fly the mission's zeppelins**: load `zeppelins.zrd.json` for the flown mission, place each record's node at its authored position, yaw and pitch, and move it along its `neindex` net under the record's own limits (`max_speed`/`max_accel`, the rate ramps, the pitch band), forward-only, never banking, with no flight model. A record whose node or net does not resolve is placed-but-held or skipped with a `zep:` line. Per-part damage and the broadside cannons are wired. It also releases the zeppelin generators a `--generators` launch gate holds, and stop points are live)
- `--zep=<chapter>/<mission>:<record>[:team=<id>][:pos=x/y/z][:net=grid]` (**graft one zeppelin record onto `--stage=empty`**, so an AI-versus-airship test needs no campaign flight. The hull comes through the `--node=` subtree path, so the zones keep their geometry and a live `AnimRuntime`, and damage, targeting and the sub-part feed wire as in a mission. Takes the chapter and mission with it. Seated at the grid origin at the record's altitude unless `pos=` says so, station-keeping on a one-node net, or flying the stage's ring on `net=grid`. `team=` reaches the parts through `AuthoredTeam`)
- `--wake-turrets` (**wake every dormant world AA emplacement at session build**. The emplacements need no flag: any chapter flight places the `ai.zrd` family and honours the shipped `ACTIVATED` values, leaving the hostile AA belt dormant until a mission script fires `WAKEUP_TURRETS`. This flag stands in for the script and wakes them all, one `turret ...: woken` line each. An emplacement with no TEAM is hostile to every pilot, the decoded loader default; each fires its authored weapon at the nearest hostile inside its `DETECTION_RANGE`. The `turrets:` census counts awake, dormant and woken)
- `--wake-generators` (**credit every script-gated generator of a campaign mission at session build**. A `--campaign=` mission starts each generator its `objectives.zrd` names in a `WAKEUP_GENERATOR` on an empty capacity budget, so nothing launches until play reaches the objective that grants the credit. This flag grants that host's whole scripted credit at build, one `egen: ... woken by --wake-generators: +N credit` line each, so a headless run sees the launches without flying the mission up to them. Inert outside a campaign mission. The cycle still paces the launches from the session start)
- `--infinite-ammo` (guns and hardpoints fire without depleting, frictionless weapon testing; the HUD ammo line shows `∞`)
- `--ammo=N` (**the low-ammo start knob**: cap every firable gun group's load and every pylon's ordnance load to `N` at rig build. It mirrors `weapons.gunAmmoCap`/`weapons.ordnanceCap` from `config.json` but survives `--det`, which drops that file (DET-8), so those knobs are otherwise unreachable in a scripted run. It caps `Capacity` as well as the live counter, so the gauge reads full at `N` and `RefillWeapons` refills to `N` on respawn. Mutually exclusive with `--infinite-ammo`: whichever appears last on the command line wins, and the loser is logged as a warning)
- `--rocket=<wep_id>` (**swap every hardpoint's ordnance** on the flown planes to the named weapon (`--rocket=wep_08`), resetting each pylon to that weapon's `CLUSTER_SIZE`. All eleven stock loadouts carry HE (`wep_06`), so a different mounted model comes either from here or from the menu's Ammo Selection screen. It beats a menu-chosen fit and applies after the bind, so a scripted run gets the type it names whatever was picked on the way in. An unknown id warns and leaves the stock ordnance. Distinct from `--loadout=<def>`, which swaps whole plane defs)
- `--hud-font-test[=STRING]` (overlay a sample string drawn with the game's own HUD bitmap font (`HudFont`, from `extracted/rimage/5pointhud*.png`) on each player's pane, in both the normal and highlight variants with a `Measure()`-width rule under each. Pair it with `--players=4` and `--screenshot`, run **windowed**, never `--headless` (SHOT-9), to confirm that a 1P view and a 4-way pane render identical glyphs scaled only by the pane factor. The default string is a mock ammo readout; an optional value overrides it. With the flag off the font is not loaded and the flight HUD is unchanged)
- `--weapon-lab[=wep_id]` (**implies `--fly`**: a chapter-world flight session whose aircraft is held in place, hosting the weapon lab panel on it (**B** hides and shows it). It fires through the session's own `ProjectilePool` at the chapter world's real surfaces. The panel writes into the bound `Loadout` and the aircraft's own trigger fires it: GUNS arm the gun groups, HARDPOINTS the pylons, and picking a hardpoint weapon re-arms every pylon and rebuilds the mounted models. The optional value mounts that weapon at launch. Ammo is infinite until the panel's toggle says otherwise. P1-only)
- `--weapon-mount=<token>` (with the weapon lab, the mount selected at launch, the gun group or pylon the trigger fires from: `g<slot>` for a gun group by its W-slot (`g1`/`g2`/`g3`/`g4`), or `pylon<n>` for a pylon (`pylon1`…`pylon8`). The default is the bank's first. The token is resolved in the bank the selected weapon implies, a `g*` token needs a gun weapon, a `pylon*` token a hardpoint weapon. There is no `all` token: the aircraft's real trigger fires one gun group at a time, as the original does)
- `--weapon-fire` (hold the **aircraft's** trigger from launch in a `--weapon-lab` session, the same trigger a pilot pulls, chosen by the selected weapon's class: a hardpoint weapon launches from the pylons, anything else fires the gun groups. For a clean firing screenshot run **windowed**, never `--headless`, a `--screenshot` under `--headless` has no rendered frame to save, SHOT-9)
- `--weapon-click[=x,y[,aim]]` (with the weapon lab, **replay one left click** at that viewport pixel on the first physics frame. No value clicks the viewport centre; a trailing `aim` is the scripted shift-click, which aims without re-parking. The lab casts its own physics ray from the camera, not `SelectionService`'s AABB pick, which skips map-scale meshes and can never select the sea or a terrain tile. It logs what it struck and re-parks the held aircraft on that ray at the panel's stand-off; a click that hits nothing leaves the aircraft where it was)
- `--weapon-target=x,y,z` (with the weapon lab, **park facing that world point** on the first physics frame, on the line from the aircraft's spawn, the mouse-free placement for a point you already have from a log or an earlier capture. The point is taken as given, so a coordinate in mid-air is a legitimate aim; a short probe ray through it names the body under it when there is one, and the readout says `(world point)` when there is not. Wins over `--weapon-surface=` and `--weapon-click=`, which win over each other in that order)
- `--weapon-surface=<registry name>` (with the weapon lab, **park facing the nearest collider carrying that surface id**. Any of the registry's fourteen names is namable: `default` `water` `seafloor` `quicksand` `lava` `fire` `player` `enemy` `airstrip` `opensesame` `death` `buildings` `dzone` `dirt`, where `dirt` is id 13, real dirt-tagged ground, not the untagged ground `default`. The search measures to the nearest vertex of the collision geometry, not to the body origin. A chapter with no such collider, or a name outside the registry, warns and leaves the aircraft at spawn)
- `--weapon-standoff=<m>` (with the weapon lab, the **parking distance** every placement uses, click, `--weapon-target=`, `--weapon-surface=`, and the panel slider's starting value. Clamped to 15–1100 m; omitted or 0 keeps the 90 m default)
- `--weapon-camera=free|orbit|<frames>` (with the weapon lab, which camera has the view. By default the lab orbits the held plane on wall time, swung with WASD and zoomed with Shift/Ctrl. `free` hands the same `Camera3D` to a `SpectatorCamera`, so the eye can fly out and watch an impact close up; **V** toggles it interactively. While the free camera holds it the controller writes nothing to the camera at all, and handing it back re-seeds the orbit from where the eye is, so neither direction jumps. A frame count toggles the two every N physics frames. With `--players>1` the lab binds to P1)
- `--weapon-cycle[=frames]` (with the weapon lab, **step the current bank's weapon list one entry every N physics frames**, default 30, the scripted twin of holding the panel's `>` button down, so the arm-and-rebuild path can be walked with nobody at the controls. Each step logs one `weapons` debug line naming the new weapon, its `FLYOUT` model, the per-pylon load, how many models mounted and how many ordnance bodies are parented to the rig; that last is the leak tripwire and must equal the mounted count however long the cycle runs. Pair it with `--frames=` and `--log=weapons:debug`)
- `--weapon-test` (implies `--det`: a parked-plane probe in the viewer that needs no world, no colliders and no frame. It mounts and fires every one of the 48 `weapons.json` entries once from every mount of its own class, a gun from each of the airframe's gun groups and a hardpoint weapon from each pylon, reports any that throw to stdout and `./.scratch/weapon_test.txt`, then quits; windowless under `--headless`. A clean run reports 48/48 fired OK, 0 errors, 0 skipped, and a skip means a real gap. It needs a parked plane, so omit `--chapter`; pair with `--plane=` for another airframe)
- `--damage-test[=name]` (implies `--det`; the **headless destructible sweep**. It builds the `--chapter` world via the freecam path, then for one live destructible per distinct `DAMAGE_SEQUENCE`-carrying def sweeps its HP from full to zero and logs **which stage effect fires at which health**, plus a `resolve✓` check that a deep descendant node, what a projectile's raycast strikes, maps back to that destructible; to stdout and `./.scratch/damage_test.txt`, then quits, windowless under `--headless`. Optional value filters destructibles by `NAME` substring, e.g. `--damage-test=ap_h2otwr`)
- `--damage-hd=<n>` (with `--damage-test`, switch to **discrete-hit mode**: apply `n` `HEALTH_DAMAGE` per hit through `AnimRuntime.DamageAt` and count hits to destruction, rather than sweeping HP. It logs the stage-crossing hits and, at death, a `swap[...]` subtree check, a `col[off N, on M]` collider census (WORLD-10), a `debris[N launched]` count. Discrete mode covers every destructible, not only `DAMAGE_SEQUENCE` ones. It forces `Collision` on, since a freecam world builds none (WORLD-9), and dumps the collidable-mesh inventory to `./.scratch/world_colliders.txt`)
- `--effects-test` (implies `--det`; build the `--chapter` world via the freecam path, then play every impact and destruction effect at the camera point, reporting whether each **resolves** and whether it **builds a puffer** (`verification.md` WORLD-12: a started def whose factory or textures are absent renders nothing), to stdout and `./.scratch/effects_test.txt`, then quit. `--det` pins the seed the runtime's dice derive from, and every effect is stopped before the next because they share `trailpuffer2`. Point-light and container effects build no puffer by construction)
- `--run-tests[=filter]` (implies `--det`; the **in-engine test harness**: run the assertion suites, print a PASS/FAIL/SKIP table and `./.scratch/test-report.json`, and **exit 1 if any suite failed**, 0 otherwise (LOG-12); an absent input reports SKIP and still exits 0 (DIAG-15). The selector is comma-separated terms, unioned in registry order: `suite:<name>` one exact suite, `tier:<name>` a tier, anything else a suite-name substring; a term matching nothing exits nonzero before any suite runs. `shard:<i>/<n>` divides that choice by `analysis/engine-suite-weights.json`. Run windowed (LOG-8))
- `--destroy=<name>` (kill a named world destructible at build, so a `--screenshot` catches its death unattended. `<name>` is a case-insensitive substring of a destructible's def, animation or anchor `cs_name`; every distinct match dies, capped at 64. It reuses the weapon-hit path, so debris, effects and audio match a rocket kill. It needs a `--chapter` world, forces no mode, and is a logged no-op in `--viewer`. In `--freecam` it auto-frames the object unless `--pos`/`--direction` placed the camera, and builds the world-effects runtime. `--damage-test` and the no-match message list the names)
- `--crash[=frame]` (force the flying players' crash rig at a fixed sim frame, default 5: `FlightController.DebugForceCrash()` fires on every rig when the clock reaches it, which is the only headless route to a crash. A no-op past the first fire, and inert without `--fly`/`--stunt`. The wreck scatters off the `Rng.Crash` stream, so pin `--seed` and the player count. It needs a `--chapter` world, the crash def coming from the mission's `AnimProgram`, so it is inert under `--stage=empty` unless `--zep=` grafts a program in. Spawned AI planes crash too, indexing the `ai_crash_*` family)
- `--menu[=<screen>]` (forces the launchscreen with other args, else bare only. A value opens it, an unknown one the top level, Instant Action's the Mode pick. A `:` suffix is its argument: `campaign-briefing` seconds, `keys` a category, `loadboard-campaign` a mission, `pauseboard` a mission and a marked count, `pauseboard-ia` a chapter and type, else a `CampaignAidScript`: counted `dulrab` moves and confirms (`4da`), a number steps. `x` is Built-in's alone; Original refuses it: exit 1, no shot. Campaign aids use scratch stores bar `campaign-fly`. Values are in `docs/org/menu-inventory.md`)
- `--skip-intro` (no boot sequence on this launch. A bare launch plays `msopen1`, `zipper` and `chap0` over the whole window before the launchscreen, in `fmv.zrd`'s own order, and any key or a left click ends the one playing and starts the next. Any argument at all already suppresses the sequence, so no test, golden or perf run sees it; this flag is for the one launch that carries no other argument, a desktop shortcut whose owner has watched the movies. It selects no content, forces no mode and still shows the launchscreen, so a launch carrying it alone is the bare launch minus the movies)
- `--intro` (a dev flag, `--skip-intro`'s opposite: play the boot sequence although this launch carries other arguments. It exists because `RunGame.ps1` and `RunDev.ps1` prepend `--volume=1.0` unless you pass a volume or `--mute`, which suppresses it, so without this flag the sequence is unreachable through the repo's launch scripts and judgeable only from an exported build. `--skip-intro` beats it, since a suppressor another flag can overrule is not one. It selects no content and forces no mode, so pair it with `--menu` on a launch that would otherwise build content, or it plays nothing)
- `--movie=<name>` (play that one cinema over the whole window, then quit; any key or a left click ends it early. The name resolves case-blind under `extracted/rof/ASSETS/GRAPHICS/MPG/` and takes `.mpg` when none is spelled, so `--movie=zipper` and `--movie=CrimFlag.MPG` both work; a name nothing matches names the folder and quits 0. No world, menu or session is built and the sound rides the Voice bus. It does **not** imply `--det`: the picture is clocked by what the audio device played. ⚠ A repo run's gain is 0, so pass `--volume=1.0` or it plays silent and says so)
- `--presentation=<name>` (a session-only menu presentation override, recorded as `SessionSpec.PresentationOverride` and never written to the saved options file. Precedence is force-Built-in, then this flag, then the saved request, then the Original default. `original` selects the Original presentation over the decoded `extracted/rof/menu_layout.json` set, else Built-in runs and each miss is logged, and it makes `--menu=` carry Original's aids. Applying a choice on either Options screen re-selects from the saved request and drops the override for the rest of the run)
- `--force-builtin` (the startup escape hatch: always resolves the active menu presentation to Built-in for this run, ahead of `--presentation=` and the saved request, and never rewrites what was saved)
- `--debug-waves=N` (launchscreen only, H16: pre-configure the first N, clamped 0-4, Instant Action wizard wave slots with a distinct non-empty load, so `--menu=waves --screenshot=` can show "N waves configured" states with nobody at the controls, the same role `--debug-join=` plays for the plane screen's join strip)
- `--debug-wingmen=N` (launchscreen only, H16: pre-configure the Instant Action wizard's wingman count, clamped 0-5, and a non-default aircraft, pairs with `--debug-join=` to screenshot the plane screen's decision-8a flown-wingmen re-clamp as players join)
- `--debug-preset=N` (launchscreen only: apply Table of Contents preset N, 0-18, and open on the wizard's step 1, so a `--screenshot` shows the FILLED wizard with nobody at the controls. This is the aid for what units cannot see, a preset applying the wrong aircraft or the wrong militia looks entirely plausible on screen. It fills the wave and wingman fields itself, so it overrides `--debug-waves=`/`--debug-wingmen=` rather than combining with them)
- `--mission=IA1` (which mission's spawns `--fly` uses; IA1 reads `ia.json` spawn_points, M0x story missions read `objectives.json` PLAYER_INIT, pair with the matching `--chapter=` so the world fits the spawn)
- `--scenario=zeppelin_run` (which instant-action scenario's spawn list, `zeppelin_run`/`dogfight_ace`/`dogfight_squadron`/`stunt_flying`/…)
- `--ia=<path>` (**fly an Instant Action mission from a hand-authored JSON file**: a plain object using a chapter's shipped `ia.zrd.json` field names plus the invented `lives`; omitted keys take the original's reset defaults. Pair it with `--chapter=`. The launchscreen wizard's in-memory `IaDef` is checked before `IaPath`. `mission_type` drives the spawn list, the ace, wingmen, waves, the objective zeppelin, the danger zones and the end condition. `lives` is per pilot: 1 faithful, N for N-1 respawns, 0 unlimited. A malformed or missing file logs and leaves the session non-Instant-Action)
- `--campaign=<profile>:<seq>` (the scripted-testing entry for a campaign session: `<profile>` is a `CampaignProfileStore` name under `user://Profiles/`, `<seq>` the `cm_sequence.zrd` flat mission index, 0-23. Both are recorded as plain values (`SessionSpec.CampaignProfile`/`CampaignMissionSeq`); loading the profile and building the mission's world is the campaign director's job. A missing or unparseable `:<seq>` keeps the profile name and warns rather than dropping the flag. There is no dedicated mode: like `--stunt`/`--vs`, a bare `--campaign=` rides the default Fly resolution)
- `--no-crash-loss` (**keep flying after losing the aircraft in a campaign mission**, the opt-out from the game rule. By default the loss ends the mission where the wreck comes to rest (`docs/formats/objectives.md`, "Win and loss"); this flag leaves the objective graph running instead. In co-op an earlier death costs that human their aircraft alone, and the last one lost ends the mission; the flag opts out of all of it. Campaign sessions only: Instant Action's lives ledger and Dogfight's respawn are untouched. Losing the aircraft is the plane's own `Downed` report (INSTR-22))
- `--spawn=N` (force spawn index N in that list; default is a random pick per launch like the original, drawn from the `spawn` stream, and the chosen spawn is logged. **`--det` defaults it to 0**, a pinned *choice* rather than a pinned dice roll: a seeded pick still moves when the mission's spawn list grows, index 0 does not. An explicit `--spawn=N` still wins under `--det`)
- `--spawn-at=x,y,z` / `--spawn-dir=x,y,z` (**deprecated**, the pre-A5 flight-only spelling of `--pos`/`--direction`; each logs `WARN [core] deprecated flag=… use=…` once per run and otherwise behaves exactly as it did. They still carry their old per-mode meaning, which `--pos` deliberately does not copy: in `--anim-lab` `--spawn-at` moves the parked stage prop as well as the camera, and in `--freecam` it moves the camera through the spawn path, where `--pos` beats it)
- `--sky-zone=zone1|zone2|zone3` (horizon zone, default `zone2`. It also selects that zone's distance fog and enables fog, whiteout and the map-edge extension in a static view. **The names are per chapter**: C1 to C4 ship `zone1`+`zone2`, **C5 ships `zone1`+`zone3`**, and a zone the chapter does not define falls back to the first its `weather.json` lists. The default is also corrected by the horizon's geometry: where the named zone builds no dome, the one that has renders. **Passing the flag explicitly opts out of that correction**, pins the fog and forces the `zone_id` gate to that zone)
- `--data-root=` (where `extracted/` lives; default the repo root. Also settable as the **`CSVM_DATA_ROOT`** environment variable, which the flag overrides. It exists so a **git worktree can run the game**: `/extracted/`, `/CrimsonSkiesGame/` and `/tools/` are git-ignored, so a worktree checkout has none of them. Point it at the primary tree and a worktree session renders **byte-identically** to that tree. It affects asset lookup only: `planes.zip`, `zrdr.zip`, `soundsh.zip`, `interp.json`, `messages.json`, `rof/`, and the per-chapter `gamez`/`texture` and mission `zrdr` paths)
- `--gamez=`
- `--textures=`
- `--zrdr=` (default extracted/zrdr.zip)
- `--interp=` (default extracted/interp.json, the boot scripts naming each chapter's clutter templates; every world build stamps them via `ClutterBuilder`, logged as `clutter: N sprites`, solid only in `--fly`)
- `--sounds=` (default extracted/soundsh.zip)
- `--zip-assets` (**read the `.zip` archives even where `ExtractAssets.ps1 -Unzip` left an unpacked sibling folder**, the asset shape an exported build ships. Process-wide: it flips `SessionPaths.ForceZipped` before the first path resolves, so the chapter gamez, texture and zrdr paths, the sounds archive and the menu pages' own lookups all take it. A path that only ever had the folder still resolves to the folder: the flag narrows the choice rather than adding a requirement. **Use it before shipping a build**, since the two shapes do not fail alike (INSTR-46))
- `--messages=` (default extracted/messages.json, the string table resolving targets.json `MSG_*` keys for objective marker text)
- `--mips=authored|generated` (**which mip levels a texture gets**, default `authored`. A chapter ships hand-drawn half- and quarter-resolution siblings (`cblock1_1`, `cblock1_2`) that no gamez material references, the original's own mip chain (`docs/formats/gamez.md`). `authored` installs those siblings as levels 1 and 2 wherever one exists, box-filtering the rest; a texture with no sibling is untouched. `generated` runs `Image.GenerateMipmaps()` all the way down. A sibling whose size disagrees with the level it claims is refused with a warning, never installed. Measure it with `--dump-mips`)
- `--graphics=original|enhanced` (the opt-in lit-world mode, default `original`. Precedence: **this flag**, then the **saved `graphicsMode` option** (`user://options.json`), then the **`graphics.mode`** config key, then `original`. Under `--det` only the flag survives, so it is the one way to ask for the enhanced path in a scripted or golden capture. It resolves once at launch (`GraphicsMode.Resolve`), announced on the `[world] graphics mode:` line, so a choice applied in Options reaches the world on the next start. An unrecognised word at any layer warns and falls back to `original`)
- `--no-fog` (**inspection aid**: neutralise the distance-fog *range* and the cloud-band whiteout so geometry stays visible to the horizon. It deliberately leaves `WorldLight` and `FOG_COLOR` **unchanged**, so a shading question stays answerable with fog off. It is implemented by pushing the **global** `csky_fog_range` to the no-op value `Weather.NoFog` uses, not by writing the `csky_fog_on` instance uniform (see `Clutter.ShaderCode`'s comment); the global is read by every fogged shader, so one write covers world, clutter, city blocks and dome. Announced in the `weather [...]` log line)
- `--no-flare` (**inspection aid**: suppress the sun's lens flare in the two chapters that author one (C2, C3). The flare's full-screen wash reaches α ≈ 0.66, survives terrain occlusion and whitens the HUD as well as the world, so in C2 and C3 *any* capture with the sun near screen centre is contaminated for unrelated comparisons. The flare is announced in the `lens flare [...]` log line either way. It is otherwise gated purely on chapter data, a gamez `sun` node in the horizon subtree plus `LensFlareTexture` slots in `support\<ch>\init.gw`, so the other six chapters need no flag)
- `--no-cockpit-pass` (**opt out of the cockpit pass**, the shipped path that draws the `cockpit1` interior in a `SubViewport` world of its own with the panel and the camera both at the origin, composited over the main view under the HUD (`Flight/CockpitOverlay`); the flag leaves the interior in the main world instead, for A/B comparison. Without the pass each dial face rounds differently once the attitude is rotated, which reads as panel jitter. **Costs a second 3D pass per player.** Not covered: an airframe swap, which rebuilds the interior and leaves the pass hiding itself)
- `--no-clutter` (**inspection aid**: skip the chapter's ground-clutter build entirely, so no tree or bush sprite cards, no C2/C5 3D city-block decorations and no clutter colliders. It is how the ground *under* the clutter becomes visible at all. It removes the population and does **not** re-place it, so nothing about the placement rule is exercised and a density question cannot be settled with it. The `clutter: skipped (--no-clutter)` line replaces the per-chapter census. ⚠ It does **not** touch the ambient cloud field (`Effects/FogVolumeClutter`, the `fvol` sprites))
- `--clutter-templates=<name>[,<name>…]` (**inspection aid**: replace the chapter's registered clutter template set with the named templates, so one district can be flown on its own. Names are the strings `interp.json`'s `AddClutterTemplates` lines carry, matched case-insensitively against the gamez's template roots. ⚠ **It replaces the template set only: the per-polygon `no_clutter` gate still applies.** **A name this chapter does not carry is not an error**, so the build line names what was asked for, what resolved and what is absent. `--no-clutter` wins if both are given)
- `--no-zone-cull` (**isolation switch**: switch off the original's gamez `zone_id` visibility gate, so every zone draws at every camera weather state. The gate hides a *lot* on purpose, in C1 the whole ground world on `zone_id 1` and the deck tiles, facades and sprite field on `zone_id 2`, so "did the gate eat it?" is answerable in one flag rather than by rebuilding. It is **not** a fidelity switch: the default is what the original does (`FUN_0056c430`). The `zone gate: N / N / N mesh instance(s) on zone 1 / 2 / 3` line prints either way. ⚠ `--sky-zone=zoneN` drives the gate too)
- `--tex-override=<name>[=<color>]` (**texture drop-in**: the named texture resolves to a flat loud colour wherever it is used. `<name>` is the PNG's base name or the truncated name the material stores. `<color>` is anything Godot parses; the default **magenta** is also the unresolvable-texture fallback, so pass an explicit colour where a lookup failure is possible. Repeat the flag for several textures. The hook is `TextureArchive.Find`, so every consumer inherits it, and on an aircraft it beats the paint substitution. A name the archive lacks, or that no material resolved, warns)
- `--tex-census[=<name>[,<name>…]]` (**texture drop-in**: every texture resolves to its own flat colour, so a frame becomes a map of which texture painted which pixel. The colour is a hash of the name (`TextureDropIn.ColorForName`), the same in every chapter and run, and `collisions` names textures that hash alike. The map is `.scratch/tex_census.json`; a `--screenshot=` run also counts the saved frame into `.scratch/tex_census_<shot>.json` by chromaticity. Counts are lower bounds: use `--no-fog` and reject tiny ones (SHOT-14). The optional value adds names the archive lacks)
- `--no-focus` (mark the run **scripted**: it does not take the foreground, and `Launcher._Ready()` hides its window with `ShowWindow(SW_HIDE)`. `--screenshot=`, `--run-tests` and the `--dump-*` probes are already scripted; this is the manual lever for a run nothing else marks. Hiding is not minimizing, so rendering and golden hashes are unaffected (SHOT-16), but the window ignores keyboard input. A window still flashes before `_Ready` runs, which `RunTests.ps1` avoids by launching on a separate desktop (`HiddenDesktop.ps1`). `--headless` saves a blank PNG)
- `--mute` (skip flight audio: a **load-time** switch, so `FlightAudio` and `WorldSounds` are never constructed at all. Nothing plays, and nothing is *counted or logged* either, `OneShotSoundsPlayed` stays 0, the probe report's `sounds` column reads zero, and the `sound one-shot:`/`crash sound:` lines never print. That blindness is a measurement trap: several errors on record surfaced only in a sounded run, and every zero-baseline taken under `--mute` had missed them. Reach for `--volume=0` instead unless the point is to skip loading the archive)
- `--volume=N` (master gain, `0`–`1`, **default `0` in a repo run, `1` in an export**, so a scripted run never sounds unless it asks and a recipient's double-click needs no flag; `RunGame.ps1`/`RunDev.ps1` pass `--volume=1.0`. Unlike `--mute` the audio subsystem still loads and plays, so at `--volume=0` every counter and log line reads as at full volume: silent but not blind. Also the `audio.volume` config key, which the flag beats; `--det` drops the config source, never the flag. An out-of-range value is clamped and an unreadable one ignored, each with a `WARN`. With `--mute` it warns)
- `--debug-collision` (draw the plane's collision probe)
- `--players=N` (splitscreen, M2.5 item 5: fly N planes 1–4 in one shared world, each with its own pane/camera/sky/HUD/device, 2P stacked top/bottom, or side by side on a window 2:1 or wider, 3–4P a 2×2 grid; P1 = keyboard + the first pad, P2–P4 the next pads in roster order; needs `--fly`/`--stunt`, falls back to 1 with a note otherwise. Each player spawns at the next index in the mission's spawn list. With `--stunt` this is a race, per-player zones/marker/clock and a shared ranked board (item 7). Launched from the menu instead, the item-6 join flow sets the count and binds the pads)
- `--debug-join=N` (menu only: seat N device-less players through the shared player setup so the aircraft screens can be screenshot without N controllers; they can never act (and the last one starts selected, so both states show), making the shot deterministic. Built-in draws them as split panes and on the join strip; under `--presentation=original` the same flag seats them on the Free Flight and Dogfight screens' seat strip and aircraft column)
- `--debug-pointer=x,y[,down][,right]` (`--presentation=original` only: stand seat 0's pointer at that authored 800x600 point for the run, a button held per word after the pair, `down` the primary and `right` the secondary the credits screen's hidden line answers, so those states are shot with nobody at the controls. Authored not window pixels, so the shot lands on the same widget whatever the window; the press arrives as an edge once and never releases, leaving the held frame standing rather than firing. **Quote it in PowerShell** (SHELL-18); fewer than two numbers stands no pointer)
- `--hold=p,r,y,thr[@dur][;…]` (scripted flight input for automated runs: `;`-separated segments with `@seconds` durations sequence the inputs, so `1,0,0,0@1.1;0,0,0,0` pulls for 1.1 s then releases, and a single segment is a constant hold. `|` separates **per-player** sequences under `--players`, the last covering any remaining players. Such runs are unattended, so a crash auto-respawns after a pause and a completed stunt race auto-rematches on the same timer)
- `--yaw=`
- `--pitch=`
- `--pos=x,y,z` (place the **subject of whatever mode is running** at a world position: the plane's spawn in `--fly`/`--stunt`, where it beats `--spawn=N`, the camera eye in `--freecam`/`--anim-lab`, the orbit eye in `--viewer`. **Quote it in PowerShell** (`"--pos=-6500,300,-1500"`) or the comma splits the argument into an array and the run dies (SHELL-18). In flight `FlightModel.Reset` applies start speed and trim, and a respawn returns to the placed pose. Under `--campaign=` a placement made during a mission intro cutscene waits for the cutscene's `WorldHeld` handoff)
- `--direction=x,y,z` (the world-space vector `--pos`'s subject faces along, any length, normalized; quote it as `--pos` is quoted (SHELL-18). In `--fly`/`--stunt` it aims the nose, and splitscreen fans the players 60 m abreast of it as `--spawn-at` does; in `--freecam`/`--anim-lab` it is the view direction, and only the direction survives because `SpectatorCamera` discards the distance; in `--viewer` it is the aim, and the orbit **pivot** is synthesized on that ray (see `--lookat`). In flight, `--direction` with no `--pos` is a no-op and says so, since the override engages only on a position)
- `--lookat=x,y,z` (**the point form of `--direction`**. It stays a point, which matters in `--viewer`: the orbit camera **pivots** on it and takes its radius from eye to pivot, so a bare direction would spin the wheel and the drag about the eye. Elsewhere it converts to a direction losslessly, against `--pos` in flight. A `--viewer --direction` therefore synthesizes a pivot, the point on the aim ray nearest the subject's centre (min radius 1 m), announced on a `[core] orbit pivot from --direction` line; with no `--pos` the centre stays the pivot and the eye swings instead)
- `--view=<1-9|back|flyby|chase|cockpit|nose>` (pin a flight camera for the run, `--fly`/`--stunt` only, else it warns and leaves chase. A digit holds a numpad snap, swung to, not cut to, and aiming the head in first person: `2` dead ahead, `4`/`6` the flanks, `8` the belly, `1`/`3`/`7`/`9` the corners below (`5` and out of range fall back). `back` is the look-behind, `flyby` the **F7** camera, re-entered on respawn, and `chase`/`cockpit`/`nose` select an **F8** view mode. Precedence: `flyby`, `back` (in first person a head snap astern, not a cut outside), a live numpad key, a pinned digit)
- `--look=x,y` (**hold a right-stick look deflection for the whole run**, the scripted twin of pushing the look stick and the only way a headless run aims it. Both components are clamped to `[−1, 1]`, `+x` right and `+y` up; a malformed pair warns and leaves the stick centred. It drives **both** halves of the look-around from one value: outside first person it swings the camera around the plane, and in `--view=cockpit`/`=nose` it aims the head, over one shared envelope (±150° round, ±60° up and down, see [`controls.md`](controls.md)). A live stick beats it while deflected)
- `--campos=x,y,z` (**deprecated**, the pre-A5 camera-only spelling of `--pos`; logs `WARN [core] deprecated flag=--campos use=--pos` once per run, then behaves identically. It never placed the plane and still does not, so it is not simply a rename of `--pos` in `--fly`. Where both reach the same camera (`--freecam`, `--anim-lab`), `--pos` wins)
- `--screenshot=<path>` (render a few frames, save a PNG, quit. Implies `--no-focus` and `--det`, so a bare `--screenshot` is reproducible with no other flags; `--no-det` opts out. The hidden window still renders, a minimized one does not (SHOT-16). Every capture prints `[core] shot pixmd5=<md5> size=<WxH> gpu=<adapter / api>`, the md5 of the raw pixel buffer rather than of the PNG (SHOT-6), which is what the golden tripwire compares; copying it into `analysis/goldens/manifest.json` turns the shot into a golden, see `analysis/goldens/README.md`)
- `--export-gltf=<path>` (export the viewer plane's mesh to glTF, then quit: the parked model in its livery with its damage baked in (hidden panel twins, off flares and point sprites pruned). Skins convert to `StandardMaterial3D` over reversed winding: baked shading survives, exterior is glTF's front face; live emitters are not part of the model. The extension picks the format: `.glb` self-contained, `.gltf` JSON plus buffer and PNGs, else `.glb`. Reads a throwaway `Duplicate()`, so the live plane is untouched; implies `--det`; works headless. Interactive twin **F10**, into `Exports/`)
- `--frames=N` (warm-up frames rendered before the shot, default 15. **Under `--det`, which `--screenshot` implies, this is exactly frame N**: the fixed clock advances one 1/60 s step per rendered frame, so N is a frame coordinate and not a wall-clock delay. The saved-shot line carries the frame reached and the counter that named it, `frame=120 clock=sim sim_frame=120 sim_time=2`; a capture with no session reports `clock=render` and counts rendered frames (`docs/tooling.md`). Two runs at the same N are md5-identical and different Ns differ, so a shot's frame number is part of what it shows)
- `--shots=N` (after the `--frames` warm-up, capture N **consecutive** frames, one per `_Process`, then quit; default 1 = the single-shot behavior at the verbatim path; N>1 writes zero-padded indexed files `foo.png`→`foo_00.png`/`foo_01.png`/… via `IndexedShotPath`, for **z-fighting debugging** since the flicker is only visible across frames)
- `--jitter=<deg>` (per-frame camera dither during a `--shots` burst, micro-orbits the eye around the framed point (`_orbit.OrbitCenter`) so a dead-still camera doesn't render bit-identical frames; default 0.15° when `--shots>1` else 0, **and 0 under `--det`** whatever `--shots` is, since the dither exists to break the bit-identity `--det` wants; `0` disables; static mode only, in `--fly` the FlightController owns the camera and the plane's motion already surfaces the fight; file `_00` is the un-jittered baseline)

## The shared selection (`--freecam` / `--anim-lab`)

**Left-click picks the object under the cursor; PgUp/PgDn walk its ancestor ladder**, Home jumps to the
outermost rung and End back to the struck leaf. A breadcrumb names every rung leaf-first with its
world-frame box, and a yellow wireframe outlines the current rung's subtree.

Neither mode builds collision (WORLD-9), so the pick casts the camera ray itself, nearest hit wins: objects
by their own AABB (a click just off a thin one can take it), meshes 350 m or wider (terrain tiles, the cloud
deck) by triangle, occluding whatever is behind them. Hide a deck tile to click the ground under the clouds.
A miss logs `select miss ... tested=N map_scale=M`. Rung names are `cs_name`, never Godot names (WORLD-8).
**Ctrl+click** also flips the struck leaf in the cyan-outlined **export set** (node lab, below).

**M** attaches the mesh lab to the selected rung's subtree and nothing else; M again, or selecting
something else, puts that subtree back as built. **C** draws the collider wireframes (see `--collision`);
in `--viewer` C stays the mesh lab's cull cycler. Scripted twin: `--debug-mesh=<spec>`.

## The node lab, `N` (`--freecam` / `--anim-lab`)

**N toggles a panel down the left edge** holding the world's `cs_name` tree, a search box and a dependency
readout for the shared selection; a click scrolls the tree to it, a row selects, a double-click frames. Buttons:
**Frame**, **Hide/Show** (flip `Visible`), **Export glTF** (the subtree to git-ignored `Exports/`), **Deps**,
**Destructibles**; for the export set **± set** (current rung), **Export set** (one GLB at world positions), **Clear set**.

The tree fills one branch at a time on expand and stops at 500 rows per branch. The dependency readout
lists the anim definitions anchored on or naming the node, its destructible pools with live HP, its mesh
and material counts, and its colliders; a source not built in this mode says so rather than reading
empty (LOG-1, WORLD-9).

- `--debug-nodelab[=spec]` (`--freecam`/`--anim-lab` only, the scripted twin: open the panel at launch
  and dump its readouts to the `ui` log category on the second frame. Tokens: `deps`, `dest` (destructibles
  view), `open` (panel only), and `node=<cs_name>` (select it first, exact match else first substring). No
  spec dumps both readouts and changes no selection.)

## The world damage lab, `F19` (`--freecam` / `--anim-lab`)

**F19 toggles a panel down the right edge that damages the destructible the shared selection is on.** It
lists that object's live HP pools with a slider, **Kill** and **Reset**. The unit of control is a pool,
not an object: a node can carry several `(def, anchor)` pools and a weapon hit resolves to exactly one,
the only reachable one. The slider is absolute HP, spent through `AnimRuntime.DamageAt`.

A kill reports the swap and the collider census before any clock advance and the debris after it
(WORLD-11), and counts colliders `off=N on=M`, never a net (WORLD-10). The first damage action builds the
effects runtime `--destroy` uses; a name outside its fixed closure fires in the log and draws nothing.

- `--debug-damage[=script]` (`--freecam`/`--anim-lab` only, the scripted twin: open the panel at launch
  and run an **ordered** script on the second frame, logging to the `ui` category. Steps, left to right:
  `node=<cs_name>`, `pool=<n>` (an unreachable pool is refused out loud), `hp=<value>`, `kill`, `reset`,
  `tick=<seconds>` and `open`. **It forces the collision build on**, so the kill's census is real.)

