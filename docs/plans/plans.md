# Completed plans

The index of every plan that has run to completion, oldest first. The plan files live in
this directory, each banner-marked `COMPLETE` with its date; they are kept for their
evidence and recorded dead ends, and read as history, not live work. **A plan sitting in
`docs/` rather than in here is live** — CLAUDE.md's "Current status" names the active one
when more than one is present.

**Starting a new plan?** Run **`/new-plan`** — it scaffolds `docs/PLAN-<name>.md` from
[`TEMPLATE.md`](TEMPLATE.md), the shared plan skeleton (status banner, ground rules, checklist,
per-item shape), with the right sections stubbed for you to fill in.

**When a plan completes:** move its file into this directory and append its row here.

| Plan | Scope | Completed |
|---|---|---|
| [PLAN-M2-polish.md](PLAN-M2-polish.md) | M2 polish run 1 (8 items) | 2026-07-17 |
| [PLAN-M2-polish-2.md](PLAN-M2-polish-2.md) | M2 polish run 2 (13 items) | 2026-07-19 |
| [PLAN-M2.5-prototype.md](PLAN-M2.5-prototype.md) | Stunt mode, launchscreen, splitscreen (7 items) | 2026-07-19 |
| [PLAN-mech3ax-cs-revival.md](PLAN-mech3ax-cs-revival.md) | The fork: anim + gamez/planes support (14 items) | 2026-07-21 |
| [PLAN-anim-playback.md](PLAN-anim-playback.md) | The animation engine (7 items) | 2026-07-21 |
| [PLAN-anim-rendering-followups.md](PLAN-anim-rendering-followups.md) | Conditions, lights, world setup, UV scroll, audio (4 items) | 2026-07-22 |
| [PLAN-docs-cleanup.md](PLAN-docs-cleanup.md) | Shrink CLAUDE.md back to an index (10 items) | 2026-07-22 |
| [PLAN-M2-polish-3.md](PLAN-M2-polish-3.md) | M2 polish run 3 (10 items; 3 and 11 closed as disproven) | 2026-07-22 |
| [PLAN-M2-polish-4.md](PLAN-M2-polish-4.md) | M2/2.5 polish run 4 (10 items; item 3 returned to backlog) | 2026-07-23 |
| [PLAN-anim-debugger.md](PLAN-anim-debugger.md) | The `--anim-lab` animation debugger (Waves 1–5) | 2026-07-23 |
| [PLAN-data-driven-crash.md](PLAN-data-driven-crash.md) | Data-driven crash: generic anim handlers + the crash plays its def (4 waves) | 2026-07-23 |
| [PLAN-doc-prune.md](PLAN-doc-prune.md) | Prune architecture.md + verification.md, comment sweep, doc-routing contract (4 waves) | 2026-07-23 |
| [PLAN-M3-weapons.md](PLAN-M3-weapons.md) | M3 weapons & destruction — guns/rockets, world destructibles, effects, HUD (Waves A–F) | 2026-07-25 |
| [PLAN-testing.md](PLAN-testing.md) | Deterministic test infrastructure + the inspect layer (18 items) | 2026-07-25 |
| [PLAN-sessionspec.md](PLAN-sessionspec.md) | `SessionSpec`: the launch args as one parsed, resolved, engine-free value (8 items, Waves A–B) | 2026-07-30 |
| [PLAN-planeviewer-split.md](PLAN-planeviewer-split.md) | PlaneViewer god-class breakup: Launcher / GameSession split (11 items, Waves A–C) | 2026-07-30 |
| [PLAN-sequencerunner-seam.md](PLAN-sequencerunner-seam.md) | Extract the sequence interpreter behind `ISequenceHost` + headless xUnit charter (2 items, Wave A) | 2026-07-30 |
| [PLAN-animruntime-role-factories.md](PLAN-animruntime-role-factories.md) | `AnimRuntime` role factories (`ForEffects`/`ForCrashRig`) + delete dead `Apply` (5 items, Waves A–B) | 2026-07-30 |
| [PLAN-m3-polish-quickwins.md](PLAN-m3-polish-quickwins.md) | M3 polish quick wins: gauges/cues, spurious errors, weapon/audio plumbing, doc drift (11 items, Waves A–D) | 2026-07-30 |
| [PLAN-m3-polish-2.md](PLAN-m3-polish-2.md) | M3 combat look & feel: anim triggers, impacts/surfaces, weapon secondary visuals, destruction bugs (14 items, Waves A–D; cockpit follow-ups → PLAN-m3-polish-3) | 2026-07-31 |
| [PLAN-m3-polish-3.md](PLAN-m3-polish-3.md) | M3 polish run 3: the 2026-07-31 cockpit-pass follow-ups — imperceptible feedback, destructible behaviour, weapon visuals, rockets (11 items, Waves A–D) | 2026-08-01 |
| [PLAN-m3-polish-4.md](PLAN-m3-polish-4.md) | M3 polish run 4: impact feedback, test affordances, blast radius (10 items; 1 disproven) | 2026-08-01 |
| [PLAN-m3-polish-5.md](PLAN-m3-polish-5.md) | M3 polish run 5: effect reachability, world-render fidelity, the instruments (10 items; 1 disproven) | 2026-08-02 |
| [PLAN-bounce-launch.md](PLAN-bounce-launch.md) | Bounce-terminated launches: solve + fly + land the 150 upward `BL-240` debris pieces (6 items, Wave A; falls split out as `BL-245`) | 2026-08-03 |
| [PLAN-deepening.md](PLAN-deepening.md) | Seven-module deepening: extract seams, move invariant enforcement from caller to module (Waves A–G; G18/G19 as design decisions) | 2026-08-03 |
| [PLAN-weapon-lab.md](PLAN-weapon-lab.md) | Weapon lab as a flight mode: held aircraft in a real world, full-rig loadout, click-to-place targeting, camera switch, `WeaponBench` (10 items, Waves A–D) | 2026-08-03 |
| [PLAN-m3-polish-6.md](PLAN-m3-polish-6.md) | M3 polish run 6: surfaces (node `active`, second material pass, `zone_set`, depth-bias rank), the anim runtime (dead delta channels, dispatch-lag re-deferral), the instruments (golden exit-1, collider probe), and the C3 magenta decision (10 items, Waves A–D) | 2026-08-04 |
| [PLAN-armour-layer.md](PLAN-armour-layer.md) | Armour layer: two-pool zone damage, armour-first with 1:1 overflow, every consumer (grazes, damage lab, gauge colour bands) reading it coherently; closes `BL-173` corrected (5 items, Waves A–C) | 2026-08-04 |
| [PLAN-m3-polish-7.md](PLAN-m3-polish-7.md) | M3 polish run 7: gauge sweep and stall-warning fidelity, the measured 2003 m flight ceiling, effect placement/reveal, `WAIT_FOR_COMPLETION` and blast falloff to a body's shape (10 items, Waves A–D; C6 disproven — census, no code; `BL-257`/`BL-258` filed on the way) | 2026-08-05 |
| [PLAN-effect-catalogue.md](PLAN-effect-catalogue.md) | Effect catalogue: `EffectCatalogue` owns the effect/crash names and derives the anchor-root set both binds stage, so adding an effect is one edit and an unstageable anchor fails the build; both hand root-tables deleted (3 items, Waves A–B; B2 landed as Decision 1's documented fallback and found `BL-262`) | 2026-08-05 |
| [PLAN-name-resolver.md](PLAN-name-resolver.md) | `NameResolver<TNode>`: name→node resolution leaves `AnimRuntime` — index/matcher/`FindAll`, symbol authority + anchoring + bind census, and the three-tier scope order with the pool as the `ownRootsOf` hook; `ResolvePath` private, the tier order structural, the whole rule set off-engine tested (3 items, Wave A) | 2026-08-05 |
| [PLAN-m3-polish-8.md](PLAN-m3-polish-8.md) | M3 polish run 8: authored animations — puffer sizes to 1×, the gun line (muzzlepuffer, flash form judged at the controls), wing lights, prop spin, splash fades, engine start/stop + slam smoke, dynamic chase distance, crash/look-behind cameras, shake decode (docs-only), the authored damage menu + def-scoped panel pairing (12 items, Waves A–D; orchestrated across parallel agent worktrees; two same-day cockpit regressions fixed) | 2026-08-05 |
| [PLAN-friends-release.md](PLAN-friends-release.md) | Friends release: export-aware roots, extraction stamp, first-ever Godot export, friend extraction kit, zip proven in Windows Sandbox (5 items, Waves A–B; two package defects caught by the clean-machine test; `BL-283` filed on the way) | 2026-08-05 |
| [PLAN-m3-polish-9.md](PLAN-m3-polish-9.md) | M3 polish run 9: the 2026-08-05 playtest batch — freecam/lab controls, the crash and fire scene, authored effects and world data (`ballflare.flt`/`apassengers`, zeppelin debris, per-chapter sky/fog zone, the authored `fogvol.zrd` cloud field replacing `CloudPuffs`) (10 items, Waves A–C) | 2026-08-06 |
| [PLAN-m3-polish-10.md](PLAN-m3-polish-10.md) | M3 polish run 10: quick-win bug fixes — crash/damage effects, the weapon gauge, the own-ship audio mix, and `MissionSetup`'s interp placement verbs (10 items, Waves A–D; six fixed, four closed as already-correct/won't-do) | 2026-08-06 |
| [PLAN-engine-free-suites.md](PLAN-engine-free-suites.md) | Engine-free suites: eight in-engine `Suites.cs` checks move to `CSVM.Tests` as `Probes.*`/plain-static facts, `StuntMission`/`Loadout` print-site enablers, `GaugeCluster`'s `StallLamp`/`ArrowSweep` structs (Waves A–B; `loadout-bind` split disproven, `tex-dropin` closed engine-bound) | 2026-08-06 |
| [PLAN-puffer-interface.md](PLAN-puffer-interface.md) | Puffer interface: `Puffer`'s six start/stop verbs (`Burst`/`TrailAdvance`/`TrailEnd`/`TrailBurnAt`/`SustainAt`/`SustainEnd`/`DriveAt`) collapse into one `Emit`/`Stop` pair over the authored mode, all four external callers migrated, the old verbs `private` (3 items, Wave A) | 2026-08-06 |
| [PLAN-vs-mode.md](PLAN-vs-mode.md) | Dogfight (splitscreen VS deathmatch, an invented third mode) + air-to-air hittability front-loading M4 A1: aircraft bodies on the shared collider boxes, attributed kills through the real damage model, aircraft-only proximity fuse + blast falloff (B14, user-directed), `--vs` + menu entry + per-pane HUD/arrows/board/rematch (13 items, Waves A–C; orchestrated across parallel agent worktrees) | 2026-08-06 |
| [PLAN-template-stage.md](PLAN-template-stage.md) | `TemplateStage<TNode>`: the pool/slot/place/reveal/hide cluster leaves `AnimRuntime` — slots/placement/identity + the `BL-288` caller-slot claim, one reveal/retire/sweep ritual driving both entry points, and the three template flags sealed as constructor state so the accepted sealing leak becomes unexpressible; 21 facts asserted off-engine (4 items, Waves A–B; B11 closed ❌ no-go — no wiring to ride, no live defect, and every fold shape re-expressed the ordering contract) | 2026-08-07 |
| [PLAN-flight-drag-lift.md](PLAN-flight-drag-lift.md) | `BL-092` + `BL-247`: a refitted piecewise drag/thrust curve (`DragExpLow`/`DragExpHigh`/`ThrottleExp`, `ThrustConst` 184→107), angle of attack as a modelled quantity feeding both a load-factor lift re-key and a `sin²α` induced-drag term, and the sustained-turn/zoom-climb probe rows that assert it against `CAP-01`/`CAP-05` (11 items, Waves A–D; the 33-vs-18.95°/s banked-turn rate gap recorded open, not fixed) | 2026-08-07 |
| [PLAN-race-grid.md](PLAN-race-grid.md) | `BL-084`: the abreast race starting grid — a whole-field `IFlightStarts` seam with `SpawnPicker` as its plain implementation, `RaceGrid` fanning every pilot symmetrically about one anchor spawn and lifting the field as one by its worst slot, selected once at session build so `--det`/solo/Dogfight never construct it and scripted spawns stay byte-identical; grid width + clearance config-backed and the per-slot spawn log made the field instrument, since a grid is not photographable (6 items, Wave A; the countdown deliberately split out as `BL-314`, blocked on the owed `PT-45`) | 2026-08-08 |
| [PLAN-ground-contact.md](PLAN-ground-contact.md) | `BL-059` item 1: the `do_intersections` ground-contact query — a swept trajectory segment ends a flagged body on real geometry and picks its `BOUNCE_SEQUENCE` branch from the struck surface, with the no-collider fallback made structural so all 13 goldens stay byte-identical. Four of the ten items came out of the cockpit checks themselves: the bounce follow-up resuming from the landing, the stopper idiom relaunching a landed piece, `MAIN_ROOT_NODE` decoded as a sentinel (90 flagged events that had never launched at all), and the settle hop that inherited the dive's momentum and tunnelled before the sweep could arm (10 items, Waves A–C) | 2026-08-09 |
| [PLAN-flight-model-rewrite.md](PLAN-flight-model-rewrite.md) | `FlightModel.cs` ported onto the `crimson.exe` decode ([`docs/org/flightModel.md`](../org/flightModel.md)): the lift demand and its ±5/9 G clamp, the drag polar in **Mach** (no induced term at all), the thrust curve behind a linear lever, per-airframe stall speed, the authored yaw-authority table, bank→yaw/pitch coupling, `return_rate` as a weathervane torque, exponential angular damping — and the three Wave-D conflicts settled: the knife-edge sag is the nose and not the lift, climb is the decoded attitude-thrust and not `ClimbGravityScale`, and the G/AOA limiters are authored inert. Most of the model's fitted constants retired for authored data and none added (15 items, Waves A–D; C24 and D33 landed as recorded disproofs with no code; the banked-turn rate, the zero-thrust drag deficit and the climb plateau recorded open) | 2026-08-09 |
| [PLAN-overcast-match.md](PLAN-overcast-match.md) | The C1 IA1 overcast sky as one picture — three defects, three waves, both reference stills matched per region *and per population*: `BL-312`'s lattice and field edge (random in-volume scatter, top-anchored cards, map-edge continuation, and the user's decode of the deck as two-regime engine trickery), `BL-303`/`BL-100`/`BL-101`'s fog (authored `FOG_RANGES` unscaled with `fogRangeFactor` deleted, a linear ramp, the dome's own authored `fog: false`, the skirt's doubled `FOG_COLOR`), and `BL-118`'s deck brightness (the mission SUNLIGHT the deck had silently stopped carrying, made regime-conditional at the fork the user took, with the `fvol` cards tuned onto the original's measured 209 plateau) (21 items, Waves A–C; `BL-118`/`BL-312`/`BL-303`/`BL-100`/`BL-101` closed, six items minted on the way, `PT-47` owed at the controls) | 2026-08-09 |
| [PLAN-object-motion-decode.md](PLAN-object-motion-decode.md) | The original's `OBJECT_MOTION` launch, contact and run-time model, decoded from `crimson.exe`'s per-frame update: `translation_range` elevation is linear (`elev/90`) not spherical, the ground-contact test is the DEFAULT column-tier query with `NO_ALTITUDE` as its lone opt-out and `DO_INTERSECTIONS` upgrading it to a full sweep, `RUN_TIME` is a ceiling with a shortened final step rather than a flight duration, and the landing response replaces the step's Y and damps velocity by 0.2 keeping sign rather than reflecting it. Folds in and closes `BL-319` and `BL-245`, re-opens `PT-46` (d), and deletes `DebrisTune`/`GravityScale` entirely with no compensating scalar restored (13 items, Waves A–D; `C10` minted mid-plan from a controls report, `BL-343`/`BL-346` filed on the way) | 2026-08-13 |
| [PLAN-weather-decompile-match.md](PLAN-weather-decompile-match.md) | The crimson.exe weather decompile landed: the runtime camera-zone state machine (ZONE1 below the deck / ZONE2 above / ZONE3 inside a fog volume) driving fog, WorldLight and the zone_id visibility gate; the deck decoded as two authored objects (world-fixed tiles at 960/1050, the zone-1 dome as the below-deck ceiling) with the plan's own "+396.4 m cap" premise disproven mid-wave (bbox midpoint, real cap +2792.8); C5's fog-volume whiteout (16 m ramps, near-black) + ZONE3 hand-off; the D-wave re-baseline closing two overcast-match residuals and re-opening the annulus derivation as BL-328; the in-band flicker as a seeded TUNE (10 items, Waves A-D; orchestrated per-item across subagents; PT-47(f) owed at the controls, CAP-36 confirmed and closed at the controls the same day, BL-328/BL-329 minted) | 2026-08-09 |
| [PLAN-puffer-engine-deltas.md](PLAN-puffer-engine-deltas.md) | The puffer engine deltas, traced out of `crimson.exe` ([`docs/org/puffer.md`](../org/puffer.md)): the two factor-of-two conventions (`SIZE_RANGE` is a radius; `DEVIATION_DISTANCE` scatters ±0.5·d), the per-particle mechanisms the engine runs and we did not (`START_AGE_RANGE` with its born-dead skip, sub-frame emission along the emitter's motion, friction damping toward an *authored* wind), the render-side rules (`NEAR_FADE`/`FAR_FADE` with the original's cross-wire reproduced, `PRIORITY`'s size nudge), and the emission accumulator — whose own item framing was wrong: the 200 m teleport guard and our per-frame batch cap sit on different branches, so the guard landed and the cap was deleted. `SCALE_SEQUENCE` closed as a disproof (authored nowhere in the install), and the invented `fireRiseScale`/`fireLifetimeScale` pair was measured at 2.11× and then DELETED on the author's same-day verdict at the controls ("~twice the height of the originals"), leaving no invented multiplier in the puffer at all (10 items, Waves A–D; `BL-336` and `BL-335` minted, `BL-218`'s `NUMBER` default settled by the ctor) | 2026-08-10 |
| [PLAN-anim-original-match.md](PLAN-anim-original-match.md) | Animation runtime matched against the decompiled `crimson.exe` interpreter (`FUN_004ecbb0` + the 47-slot dispatch table): `CALL_SEQUENCE`/`STOP_SEQUENCE` semantics, the non-nesting-aware `IF` scan, the `LOOP` rewind and its deliberately-kept sim-time carry, `START_TIME ANIMATION` reading the instance clock, `FBFX_COLOR_FROM_TO`'s full-screen flash and the four remaining unhandled event kinds triaged to zero handlers, proved end to end by the `ordnance-burst-timeline` suite (HE/flash/sonic) (15 items, Waves A–D; `B16`'s `RANDOM_WEIGHT` table closed ❌ disproven — unreachable in shipped data; `BL-333`/`BL-334` filed on the way; D32's single golden re-baseline moved exactly the three shots B12/D31 already named) | 2026-08-10 |
| [PLAN-clutter-uv-placement.md](PLAN-clutter-uv-placement.md) | Clutter placement rewritten off the crimson.exe decode: `GroundInfo`'s scalar `Period` replaced by the ground quad's two-axis signed UV map, the world-space grid stamp replaced by a per-triangle UV-lattice walk (`FUN_004dd6e0` steps 1-11), the `no_clutter` gate landed with `BuriedClutterDistricts` retired, a `templates.zrd` reader applying `substitute` and `scale_range`, and `far_fade_range`/`rotation_range`/`translate_uv_range` surveyed and deferred (`BL-337` minted; the other two default everywhere and needed no backlog entry) (11 items, Waves A-C; C1's tree positions confirmed exact at the controls; `BL-305`'s C5 packing closed on the user's flyover north of `brooklynbridge`, the founding nadir pose having turned out unscoreable against the original's frame) | 2026-08-10 |
| [PLAN-crash-surface-id.md](PLAN-crash-surface-id.md) | Crash and touchdown selection replaced with the original's surface-id table: the material `soil` field carried through to the collider as the original's numeric surface type id, the compiled+level-supplied surface-name registry (`ZBD/zrdr.zbd`'s `LoadSoils` list), crash-def and `touchdown_*` graze selection both replaced with the original's indexed cascade (`vector[id]`, falling back to slot 0 — `player_crash_default`/`touchdown_default` play as the FALLBACK arm, not `_dirt`, correcting the build's backwards default), and the mid-air destruct modelled as the original's two-stage split (a canned no-`player_crash_*` destruct with no trigger in M3, documented not wired); dead `CrashSurface` enum retired and `BL-059` closed (7 items, Waves A–C; A1 measured the faithful selection's cost before B11 committed to it, finding the sea splash survives and the ground crash goes from `_dirt` to `_default` on 64–97% of collidable area per chapter) | 2026-08-13 |
| [PLAN-surface-id-weapons.md](PLAN-surface-id-weapons.md) | Part two of the surface-id work: the weapon `IMPACT` table re-keyed from the texture-derived `SurfaceClass` onto the original's numeric surface id at parse time and at impact, with the decoded empty-row rule shipped faithfully (an id a weapon authors no row for plays **nothing** — no fall back to row 0, unlike the neighbouring crash cascade), `SurfaceClass` deleted at every call site including the two water predicates (now the id `== 1` test the original makes), and the `--collision=show` overlay re-coloured by the surface id each body **resolves** to, legend and all. `PLAN-crash-surface-id` Decision 3 ("weapons are a different name space") disproven by decode before Wave B; `BL-344`/`BL-345` closed, `BL-347` (the third registry-indexed vector, `ai_crash_*`) minted on the way (5 items, Waves A–C; A1 counted the shipped `IMPACT` blocks and disproved the write-up's `fault` block, the shipped towers and hangar walls turning out to carry soil `default` so the `buildings` row is unreachable on them, and C21's own C4 `col_water`-on-soil-0 premise disproven and relocated to C1/C1B/C3) | 2026-08-13 |
| [PLAN-ai-flight.md](PLAN-ai-flight.md) | AI flight, the plant then the pilot: the decoded AI force path ported off the player one (nose-aligned airflow, skipped weathervane, the AI speed floor, ground blow — C21–C23), `BL-330`'s authority ramp and the reverse-authority factor shared by player and AI alike (C24), `BL-172`'s bounce restitution decoded player-only (C25), the original's own AI control law decoded from `crimson.exe` and ported to replace the placeholder bank-to-turn law (D31, E41–E42, retiring `PatrolThrottle`/the altitude leash/the placeholder gains), and the at-the-controls verdict on both arms (F51–F52): AI flight reads well except for a confirmed limit-cycle in `AiControlLaw.Steer`'s bank branch on a near-zero heading error (`BL-387`) and a soft low-speed nose-down note (`BL-388`), both filed rather than fixed here (14 items, Waves A–F; merged against a concurrent patrol-net branch mid-plan, keeping the decoded law over the placeholder it replaced) | 2026-08-15 |
| [PLAN-sticky-bullets.md](PLAN-sticky-bullets.md) | `BL-342`: the player's per-muzzle gun aim assist decoded out of `crimson.exe` (`org/aim-assist.md`) and built — candidate scan/lead solver/plane-local smoothing/1° scatter — plus removal of the `CANNON_SPREAD` scatter CSVM wrongly applied that the original never did, requalified as the assist's acceptance cone; gated on every human pilot (splitscreen, not pane 1) with the shooter-authoritative invariant recorded for a later networking milestone (7 items, Waves A–C; `PT-43` confirmed the assist reads right with no retune, `BL-301` picked up the spawn-camping and 4-player-HUD findings that fell outside scope) | 2026-08-13 |
| [PLAN-M4-ai.md](PLAN-M4-ai.md) | Milestone 4 — Game AI: the shipped AI data made live — dogfighting AI pilots (the nine-mode machine, maneuver library, target ranking, skill-interpolated gunnery, the rubber-band assist), net patrols and egen generators, carried + world turrets, zeppelins (motion, multi-zone damage, ballistic broadsides, fighter launch), the combat-voice runtime, the `ai_crash_*` surface family, and the decoded whole-vehicle damage overflow with the targeting HUD extended to AI hostiles (22 items, Waves A–H; B7 closed disproven; orchestrated per-item across agent worktrees; two same-day at-the-controls corrections — the lay-off entry geometry and the D14 overflow — decode-confirmed and landed mid-run) | 2026-08-14 |
| [PLAN-perf-hitches.md](PLAN-perf-hitches.md) | Frame hitches: an always-on `HitchMonitor` instrument (rolling-baseline + floor trigger, ring buffer, GC/counter deltas, `--hitch-inject=`), `PerfSample` breadcrumb attribution seeded across debris/spawn/pool/material/load sites, the `PerfHud` F14 readout (compact + full tier, frame-time strip), the A/B rig's hitch fields, and the instrument's first diagnosis: the damage-lab hitch traced to first-use synchronous emitter construction (not GC, not allocation volume), filed as `BL-355` (16 items, Waves A–E, G; E13 closed ❌ disproven — a `--damage=` preset structurally cannot trip the monitor, since it finishes before the build-time grace window starts) | 2026-08-14 |
| [PLAN-instant-action.md](PLAN-instant-action.md) | Instant Action as a configurable mission — pick an environment, one of four mission types (dogfighting an ace/a squadron, stunt flying, attacking a zeppelin), aircraft, wingmen and up to four enemy waves, flown to a scored win or loss: the decoded option sets/defaults/wave sequencer/wrap-up counters, a real team model replacing `AimAssist`'s pilot-index stand-in, and the launchscreen's five-step Instant Action wizard converging with `--ia=` on one `InstantActionDef` build path (16 items, Waves A–H; A1 corrected mid-plan on the C1C/C2B environment pairing; F12 disproved the standing "wave logic wakes the zeppelin" note) | 2026-08-15 |
| [PLAN-splitscreen-polish.md](PLAN-splitscreen-polish.md) | The whole Splitscreen backlog theme: per-pane draw rules (puffer fade, screen wash, world-light budget), gameplay "the player" rules resolved per nearest human/attacker, audio one-shots with mix gain and a chosen listener model, seats and input (phantom-device pad assignment, camera views/look-back, splitscreen pause, per-seat spectator control), debug-tooling pane binding, and the owed chrome playtest confirming `HudMetrics` damping/join-lock feel/tag-gutters/`SpawnAbreast` (17 items, Waves A–F; the chrome playtest also minted `BL-389`/`BL-390`/`BL-392` and, out of scope, `BL-391`) | 2026-08-15 |
| [PLAN-targeting.md](PLAN-targeting.md) | Player-selected targeting replacing the shipped auto-nearest marker: `docs/org/targeting.md`'s decode (selection order, class model, the gun-reach bracket gate), `TargetRef`/`TargetPool`/`TargetSelection` as the pure selection model, `D-pad Up` tap/hold + a curated `T`/`Y`/`U`/`I`/`O` keyboard set + `--target=`, `TargetHud` split out of `VersusHud` drawing the original's own marker shape, `--debug-markers`' extended identity/health string, and a pinned golden (11 items, Waves A–D; four deferrals minted as `BL-394`–`BL-397`, `BL-357` gained corroboration) | 2026-08-16 |
| [PLAN-ordnance-types.md](PLAN-ordnance-types.md) | The decoded ordnance runtime (`BL-406`): two-vector launch inheritance/acceleration/end conditions, guidance + the beeper/seeker pair, the quadratic blast with cover and the 32 cap, the four no-damage disabling types with the human-wash/AI-stun split, the smoke-screen trap, the shootable targetable torpedo flyout, the `DAMAGES_ZEPPELIN` gate, and the victim-routed per-pane wash channel (22 items, Waves A-F; PT-62-PT-73 signed off at the controls, PT-74(c)/PT-75/PT-76 live on as ordinary playtest rows; four at-the-controls fixes landed mid-sign-off and one row expectation was overturned by the decoded owner exclusion) | 2026-08-20 |
| [PLAN-ai-damage-and-engine-audio.md](PLAN-ai-damage-and-engine-audio.md) | `BL-385`: AI aircraft showed no damage and died silently. Per-entry damage staging replacing the name-keyed `_applied` latch (absorbing `BL-384` item (3)), the AI spawner given both phases of the `DamageVisuals` wiring, positional AI engine audio on the decoded 2000-unit cull, and four corrections to the player's own engine-audio model that the decode refuted. Wave D then rebuilt the death sequence against two reference recordings after the first three waves shipped a misreading: the self-named DESTROY anim split from the material-indexed GROUND-IMPACT one, `Callback` 15/16 as the handover protocol that lets a dead hull keep flying itself for three seconds, the parachutist, and the player's own four-piece breakup (26 items, Waves A–D; five prior readings refuted, D24 closed ❌ disproven with its one real deviation landed, `BL-421`/`BL-422` minted by the closing playtest and left open) | 2026-08-20 |
| [PLAN-c3-balloon-kill-chain.md](PLAN-c3-balloon-kill-chain.md) | `BL-348`: C3/M02's balloon-battery kill chain (`tbaseN`/`bontN`/`b_turretN`), re-flown against the current build and closed disproven — none of the four filed symptoms (over-trigger, anchor drag, dropped rise-top call, hanging turret kill) reproduced (6 items, Waves A–C; B2–B5 each closed ❌ on the re-fly's evidence, no code landed) | 2026-08-21 |
| [PLAN-flightcontroller-deepening.md](PLAN-flightcontroller-deepening.md) | FlightController deepening: six concepts out of the flying node behind six seams — `IWorldQuery` over all eight physics-query sites (and `TurretController`'s LOS), `IFlightInputSource` resolved once at bind plus a scripted adapter, `FlightHud` fed one per-frame struct with the state-to-readout mapping pinned off-engine, `FlightModel.Collide` owning the decoded slide/restitution/kick, `AircraftContactResolver` returning one `ContactOutcome` with the outcome table asserted and the cross-instance grace write replaced, and `AircraftLifecycle` reporting transitions with its table asserted — 71 new off-engine assertions, 15 public mutable fields off the node's surface, no golden moved (13 items, Waves A–F; orchestrated per-item across subagents) | 2026-08-21 |
| [PLAN-animruntime-dispatch.md](PLAN-animruntime-dispatch.md) | AnimRuntime dispatch-axis families (architecture-review candidate A): the ~1,300-line dispatch region carved into three internal families behind the unchanged facade - Anim/SoundChannel.cs (SOUND_NODE/SOUND, the late-failure census), Anim/LightChannel.cs (LIGHT_STATE/LIGHT_ANIMATION and the light tick), Anim/PoseChannel.cs (the nine OBJECT_* kinds, the opacity/fade machinery, the motion-builder role handed to MotionSet) - with the explicit switch kept as the router, the _rest seam settled as the existing RestOf method, and the census-driven surface trim taking AnimRuntime from 96 public declarations to 60 (dead observability deleted, WorldSession-only and test-only tiers internal; the census's object-initializer blind spot caught Lights in the act). docs/adr/0001 records the axis choice over the declined mode split (5 items, Waves A-D; orchestrated per-item across subagents, every wave verified by a full suite + golden pass, no golden moved) | 2026-08-21 |
| [PLAN-cockpit-view.md](PLAN-cockpit-view.md) | `BL-080`: the original's two first-person views — Cockpit (mode 6, interior drawn, 80° H FOV) and Nose (mode 7, interior hidden, 60° H) as camera modes beside `Views[]`, placed at the per-plane `cockpit_camera` marker with the −4.70° head-pitch constant and inherited wobble, the `cockpit1` interior with its shipped-active damage/lamp states parked and `pcdp4/6` panels driven off the exterior pair's own injure entries, the decoded head-look controller (numpad snap cluster per the original's keybinds, free-look, autohead off `player.json`), and the `cockpit_engine_sound` swap closing `BL-161` (9 items, Waves A–E; orchestrated per-item across subagents; the Nose "locked head" and mode-7 force-off readings retired mid-plan, the numpad look cluster identified as BL-150's real mechanism, BL-431–BL-436 minted at close) | 2026-08-23 |
| [PLAN-hangar.md](PLAN-hangar.md) | The hangar, Build Custom Plane (`BL-354` absorbing `BL-067`): the original's nine-screen flow in the launchscreen idiom over one scratch `CustomPlaneDef` — airframe/engine/armour/guns/hardpoints/paint/name/purchase with the decoded stat, gun, engine and swatch/pattern tables rendered verbatim, the capacity/no-engine gate, JSON persistence in `user://Planes/` with the 204-byte original format import-only (proven against seven genuine saves), customs in every human picker with the index-11 auto-select, and the flight join (guns/pylons/armour zones/paint/name real; engine power the authored registry override; nitrous inert, untraced). Four decode passes settled the callback dispatchers, the record layout, the armour/engine combat consumers (weight and the power rating proven hangar-only), the 27-swatch/14-pattern paint model with the icon-layer preview, and disproved the per-airframe slot-count premise; four at-the-controls rounds landed as Wave E (the string-206 defaults ask, Enter-picks interaction, the armour ×5 display, plane deletion, decal icons, the totals row) (27 items, Waves A–E; orchestrated per-item across subagents, every wave battery-verified, no golden moved) | 2026-08-24 |
| [PLAN-flight-model-parity.md](PLAN-flight-model-parity.md) | Flight-model parity against `crimson.exe`: the fourteen open flight/collision backlog items settled by decode rather than tuning — the dense atmosphere band below 2000 m, nose-chase and engine torque/roll-to-pitch/turbulence disproven, the linear throttle lever, the pitch fade, G ramp and the AOA window enabled at decoded strength (user decision), the far-field AI plant, the six control-surface name lists with the Fury rudder kept as a named exception, the placement-rewrite contact path with the graze invincibility fixed at the controls, sweep cadence, nitro, the autogyro's softness as wing loading, and the standing ruling that the executable overrules footage always (no conflict class), closed by the eleven-airframe parity ledger with every mechanism and envelope row classified decoded / intentional exception / unsupported (15 items, Waves A–E; D31–D33 closed ❌ disproven; E43 minted mid-plan; `BL-442` filed, `BL-121` reduced; orchestrated per-item across subagents, three golden re-pins user-reviewed) | 2026-08-24 |
| [PLAN-m4-polish.md](PLAN-m4-polish.md) | Ten small follow-ups off the parity lane, half of which the decode disproved rather than implemented: the dead AI open-loop throttle deleted (its `playerPosition` seam was never assigned by anything), the one-sided negative `C_L` ceiling shown unreachable because both call sites hand `FUN_0041abd0` a vector LENGTH, the dead hull's frozen lever confirmed already correct and now asserted every frame, and both halves of the perf-flake item closed against a script that had never had the short-circuit the entry described. What landed as behaviour: the per-contact camera shake (block 5, `min(|v| x severity x 0.03, 0.15)` rad, player-only and crashed-gated, a graze included) and the damaged-engine gate moved off "any damage at all" onto the decoded disabled-systems mask, whose bit is the worst critical zone's health-only fraction below the def constructor's 0.25. Plus the flight dump made cross-host hash-equal (at the cost of a nearest-10-m delta-alt, recorded in INSTR-19), `CheckCommentCaps.ps1` rooted on the tree it lives in, `ZzBaselineDump` renamed, and `BL-266` (b)/(d) rewritten against the shake decode, which surfaced that CSVM's explosion shake kicks where the original's is decoded to zero (10 items, Waves A-B; orchestrated per-item across subagents in parallel worktrees; `BL-223` deleted as disproven by B14's decode, `BL-459`/`PT-84` minted at close; no golden moved) | 2026-08-24 |
